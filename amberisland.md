# WebScore Navigator

WebScore Navigator 是一个轻量级、社区驱动的技术资源导航与外部数据聚合站点，专注于为开发者和技术决策者提供实时、结构化的公开数据源索引。本项目不提供数据存储或后端计算服务，而是作为一套严谨的 URL 路由与展示框架，将分散在多个独立域名下的公开比分、统计与资讯页面整合至统一的本地开发环境中，便于开发者快速集成外部数据接口或构建前端可视化演示。

项目定位为技术演示与资源汇集工具，目标用户包括前端开发工程师、数据可视化爱好者、开源贡献者以及需要快速接入外部公开数据源的原型设计团队。通过本项目提供的标准化导航结构，用户可以在本地启动一个轻量级服务，一键直达数十个外部数据页面，无需记忆繁琐的域名后缀，同时为后续扩展自定义数据解析逻辑提供清晰的代码组织范例。

## 功能概览

- **分组式外链路由**：将外部资源按赛事类型、数据维度等逻辑分组，提供清晰的分类导航页面。
- **静态站点生成**：基于纯静态 HTML 与 CSS 构建，无需后端服务即可运行，便于部署至任意 Web 服务器或 CDN。
- **本地开发热重载**：集成开发服务器，支持代码修改后自动刷新页面，提升调试效率。
- **URL 规则校验中间件**：内置简单的请求拦截逻辑，可验证外部链接的域名格式是否符合预设规则，辅助开发阶段排查链接错误。
- **可配置数据源映射**：所有外部链接集中维护于单一配置文件，支持增删改查，无需修改页面模板逻辑。
- **响应式网格布局**：导航卡片适配桌面、平板与移动设备屏幕，保证不同终端下的可访问性。
- **访问状态模拟指示器**：为每个外部链接提供模拟的可用性状态标记（基于本地配置），方便快速识别潜在失效资源。
- **基础 SEO 元数据注入**：每个分组页面自动生成标题与描述标签，便于搜索引擎抓取和书签管理。

## 应用场景

1. **前端原型演示**：开发者在构建数据仪表盘或赛事信息看板时，可使用本项目快速填充外部数据源链接，聚焦于 UI 交互设计，而非数据采集。
2. **开源项目文档站附属导航**：作为技术文档站点的一部分，提供“相关资源”页面，将多个外部数据服务统一陈列，减少用户检索成本。
3. **内部工具链整合**：技术团队可将本项目作为内部浏览器起始页，聚合团队常用的公开数据查询入口，提升日常开发信息获取效率。
4. **教学实训案例**：在编程培训或高校课程中，作为静态路由与配置管理的入门示例，帮助学生理解 URL 组织与模块化设计思路。
5. **个人书签替代方案**：替代浏览器收藏夹，以项目形式托管并版本化管理常用外部链接，便于多设备同步与分享。

## 快速开始

以下步骤将帮助你在本地环境中快速启动 WebScore Navigator 服务。

```bash
# 1. 克隆代码仓库
git clone https://github.com/webscore-navigator/webscore-navigator.git

# 2. 进入项目目录
cd webscore-navigator

# 3. 安装依赖（使用 npm 或 yarn）
npm install

# 4. 启动本地开发服务器
npm run dev
```

执行完毕后，在浏览器中访问 `http://localhost:3000` 即可查看导航页面。配置文件位于 `config/routes.json`，可按需修改外部链接列表。

## 安装要求

本项目的运行依赖以下工具与库，请确保开发环境已正确安装。

| 依赖 | 必需版本 | 说明 |
|------|----------|------|
| Node.js | >= 16.0.0 | 运行时环境，用于执行构建脚本与开发服务器 |
| npm | >= 7.0.0 或 yarn >= 1.22.0 | 包管理器，用于安装项目依赖 |
| Vite | >= 4.0.0 | 前端构建工具，提供开发服务器与打包能力 |
| ESLint | >= 8.0.0 | 代码检查工具，用于维护代码规范（开发依赖） |
| Prettier | >= 2.0.0 | 代码格式化工具（开发依赖） |
| 现代浏览器 | 最新两个版本（Chrome, Firefox, Edge, Safari） | 运行时环境，支持 ES6+ 与 CSS Grid/Flexbox |

## 文档导航

为帮助不同角色的使用者快速定位所需信息，项目文档按以下层级组织。

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 入门指南 | `docs/getting-started.md` | 如何快速运行项目？配置文件在哪里修改？ |
| 配置参考 | `docs/configuration.md` | routes.json 的字段含义是什么？如何新增分组或链接？ |
| 开发指南 | `docs/development.md` | 如何参与开发？代码提交规范是什么？如何调试中间件逻辑？ |
| 部署手册 | `docs/deployment.md` | 如何将站点部署至 Vercel、Netlify 或 Nginx？ |

## 资源列表

本项目导航页面默认收录以下外部数据资源，按分组类别陈列。所有链接均以原始格式原样引用，未做任何协议或域名改写。

### 足球比分类

- <code>qiutanzuqiubifenb.org.cn</code>
- <code>qiutanzuqiubifenc.org.cn</code>

### 篮球比分类

- <code>lanqiubifena.org.cn</code>
- <code>lanqiubifenb.org.cn</code>
- <code>lanqiubifenc.org.cn</code>

### 综合比分类

- <code>qiutanbifena.org.cn</code>
- <code>qiutanbifenb.org.cn</code>
- <code>qiutanbifenc.org.cn</code>

### 比分网汇总类

- <code>bifenwanga.org.cn</code>
- <code>bifenwangb.org.cn</code>

## 项目结构

项目采用模块化目录组织，核心代码与配置分离，便于维护与扩展。

```
webscore-navigator/
├── config/                         # 全局配置目录
│   └── routes.json                 # 外部链接路由映射表（分组、标题、URL）
├── src/                            # 源代码主目录
│   ├── assets/                     # 静态资源（图片、字体、公共样式）
│   │   ├── styles/                 # 基础 CSS 与主题变量
│   │   └── icons/                  # SVG 图标集
│   ├── components/                 # 可复用 UI 组件
│   │   ├── NavCard/                # 导航卡片组件（含状态标记）
│   │   ├── GroupHeader/            # 分组标题组件
│   │   └── Footer/                 # 页脚组件（含版本信息）
│   ├── pages/                      # 页面级入口文件
│   │   ├── index.html              # 主导航页面
│   │   └── 404.html                # 自定义错误页面
│   ├── utils/                      # 工具函数库
│   │   ├── urlValidator.js         # URL 格式校验函数（基于预设规则）
│   │   └── configLoader.js         # 加载并解析 routes.json
│   └── main.js                     # 客户端入口脚本（初始化渲染）
├── scripts/                        # 构建与辅助脚本
│   └── generate-sitemap.js         # 自动生成站点地图（用于 SEO）
├── docs/                           # 项目文档（详见文档导航）
├── tests/                          # 单元测试与集成测试
│   ├── unit/                       # 工具函数单测（使用 Vitest）
│   └── e2e/                        # 端到端测试（使用 Playwright）
├── .eslintrc.json                  # ESLint 规则配置
├── .prettierrc                     # Prettier 格式化配置
├── vite.config.js                  # Vite 构建配置
├── package.json                    # 项目依赖与脚本定义
└── README.md                       # 项目总览文档（本文件）
```

## 贡献指南

我们欢迎任何形式的贡献，包括但不限于新增外部链接、优化 UI 样式、完善文档或修复潜在缺陷。请遵循以下步骤提交您的修改。

1. 在 GitHub 上 Fork 本仓库，并克隆至本地开发环境。
2. 创建一个新的功能分支，分支命名遵循 `feature/简述修改内容` 或 `fix/简述修复问题` 的格式。
3. 在 `config/routes.json` 中按既定 JSON Schema 添加或修改外部链接条目，确保 URL 格式与分组结构正确。
4. 若涉及 UI 变动，请在本地运行 `npm run dev` 预览效果，并确保页面在不同视口宽度下表现正常。
5. 提交前运行 `npm run lint` 和 `npm run test` 确保代码风格与单元测试通过。
6. 提交 Pull Request 至主仓库的 `main` 分支，并在描述中清晰说明修改目的与测试结果。

## 常见问题

**问：项目是否提供数据缓存或代理转发功能？**

答：不提供。本项目仅作为静态导航页面，所有外部链接在用户点击后直接由浏览器发起请求，不经过任何中间服务器处理。如需代理或聚合数据，请自行扩展后端服务。

**问：配置文件中的 URL 发生变化时，是否需要重新构建整个项目？**

答：如果仅修改 `config/routes.json` 中的链接文本或地址，且不涉及页面模板结构，只需重启开发服务器（或重新构建生产包）即可生效。对于生产部署，建议执行 `npm run build` 重新生成静态文件。

**问：如何添加自定义分组或调整分组显示顺序？**

答：编辑 `config/routes.json` 文件中的 `groups` 数组。每个分组对象包含 `name`（分组标题）和 `links`（链接数组）。调整数组元素的顺序即可改变分组在页面上的渲染顺序。链接数组内的对象字段包括 `title`（显示名称）和 `url`（完整外部地址）。

## 许可证

本项目采用 MIT 许可证。您可以自由使用、修改、分发本项目的源代码，包括用于商业目的，但需保留原始版权声明。详见项目根目录下的 `LICENSE` 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:54:30
