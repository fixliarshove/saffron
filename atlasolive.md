# TechLink Navigator

TechLink Navigator 是一个面向开发者、技术研究人员与开源爱好者的高质量外链资源导航与聚合系统。该项目定位于解决技术信息分散、优质资源难以检索、领域垂直站点缺乏统一入口的问题，通过人工筛选与社区共建的方式，构建一套可长期维护、可扩展的技术资源索引体系。项目主要服务于需要频繁查阅特定领域资料、关注行业动态、以及希望系统化组织个人知识渠道的中高级技术人员。

## 功能概览

- **多维度资源分类**：按技术领域、资源类型、内容形态对收录链接进行细粒度标签化管理，支持快速筛选与定位。
- **外链存活状态监测**：内置周期性 HTTP 状态检测机制，对收录域名进行可用性检查，自动标记异常链接，辅助维护者及时清理或更新。
- **自定义标签与备注系统**：允许用户为任意链接添加自定义标签、备注说明及个人评分，便于团队协作与个人知识管理。
- **模糊搜索与快速跳转**：提供基于标题、标签、域名关键词的轻量级模糊搜索，支持键盘快捷键快速打开目标外链。
- **导入导出标准格式**：支持 JSON / YAML / CSV 格式的链接数据批量导入与导出，便于迁移、备份或与其他工具集成。
- **访问统计与热度排序**：记录各链接的点击次数与最近访问时间，支持按热度、添加时间、更新时间等多种方式排序展示。
- **响应式布局与夜间模式**：适配桌面与移动设备，提供浅色/深色主题切换，提升长时间使用体验。

## 应用场景

- **技术团队内部知识库外链整合**：研发团队可将日常常用的官方文档、技术博客、API 参考、设计规范等分散链接统一纳入 TechLink Navigator，形成团队共享的资源门户，减少重复查找时间。
- **开源项目文档中的外部参考索引**：开源项目维护者可以在项目 README 或 Wiki 中引用 TechLink Navigator 作为外部资源索引页，帮助贡献者快速找到相关的背景资料、依赖项目或社区讨论区。
- **个人开发者信息聚合与学习路径管理**：独立开发者可利用本系统整理不同技术栈的学习资料、视频教程、问题排查站点，并按照学习阶段或兴趣方向进行分类，构建个人化的技术成长地图。
- **技术社区或媒体平台的合作链接库**：技术媒体、播客或社区运营方可使用本系统管理合作方链接、投稿来源、行业报告下载页等，作为公开资源或内部运营工具。

## 快速开始

以下命令适用于 Linux / macOS / Windows WSL 环境，可完成项目克隆、依赖安装及开发服务器启动。

```bash
# 克隆代码仓库
git clone https://github.com/techlink-navigator/tln-core.git
cd tln-core

# 安装依赖（使用 npm，若使用 yarn 或 pnpm 请相应替换）
npm install

# 启动本地开发服务（默认监听端口 3000）
npm run dev
```

启动成功后，访问控制台输出的本地地址（通常为 http://localhost:3000）即可开始使用。生产环境部署请参考 `docs/deployment.md`。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
| :--- | :--- | :--- |
| Node.js | >= 18.17.0 | 运行时环境，建议使用 LTS 版本 |
| npm | >= 9.6.0 | 包管理器，或使用 yarn 1.22+ / pnpm 8.0+ |
| PostgreSQL | >= 14.0 | 主数据库，用于存储链接数据、标签及统计信息 |
| Redis | >= 6.2 | 缓存与会话存储，用于提升查询性能及状态检测队列 |
| Git | >= 2.30 | 版本控制工具，用于克隆及后续更新 |
| Docker (可选) | >= 20.10 | 若使用容器化部署方式，需安装 Docker 及 Compose |

## 文档导航

| 层面 | 目录 | 回答的问题 |
| :--- | :--- | :--- |
| 用户手册 | `docs/user-guide/` | 如何添加链接、分类管理、搜索使用、个性化设置等日常操作 |
| 部署运维 | `docs/operations/` | 生产环境部署方案、环境变量配置、数据库迁移、日志与监控 |
| 开发指南 | `docs/development/` | 项目架构说明、API 接口规范、前端组件开发、测试用例编写 |
| 贡献规范 | `docs/contributing/` | 代码提交规范、PR 流程、社区行为准则、新增资源分类的标准 |

## 资源列表

本列表收录项目初始整合的全部外链资源，按内容主题划分小节。所有链接均保持用户提供时的原始格式，未做任何协议或域名修改。

### 综合内容与基础分类

- <code>oumeirihanyi.org.cn</code>
- <code>guochanshuqiyiquerqu.org.cn</code>
- <code>rihanzhongwenzimudiyiye.org.cn</code>

### 特定专题与区域

- <code>nantongwuma.org.cn</code>
- <code>yazhouyikaerka.org.cn</code>

### 产品与内容类型

- <code>guochanheisi.org.cn</code>
- <code>daxiangjiaopapa.org.cn</code>

### 视觉与媒体相关

- <code>oumeijiqingzaixianguankan.org.cn</code>
- <code>shunvse.org.cn</code>
- <code>yazhouqingseyiquerqu.org.cn</code>

## 项目结构

项目采用分层架构设计，前端基于 React + TypeScript，后端基于 Node.js + Express，以下为核心目录及主要职责。

```
tln-core/
├── backend/                        # 后端服务代码
│   ├── src/
│   │   ├── api/                    # RESTful API 路由及控制器
│   │   ├── services/               # 业务逻辑层（链接管理、检测、统计）
│   │   ├── models/                 # 数据库模型定义（Sequelize / TypeORM）
│   │   ├── workers/                # 后台任务（链接状态检测、缓存刷新）
│   │   └── utils/                  # 通用工具函数（日志、验证、HTTP 客户端）
│   ├── tests/                      # 单元测试与集成测试
│   └── package.json
├── frontend/                       # 前端应用代码
│   ├── src/
│   │   ├── pages/                  # 页面级组件（首页、分类、搜索、详情）
│   │   ├── components/             # 可复用 UI 组件（导航栏、链接卡片、标签）
│   │   ├── hooks/                  # 自定义 React Hooks
│   │   ├── stores/                 # 状态管理（Zustand / Redux）
│   │   └── styles/                 # 全局样式与主题变量
│   ├── public/                     # 静态资源
│   └── package.json
├── docs/                           # 完整文档（用户手册、开发指南、部署）
├── scripts/                        # 构建、迁移、数据导入等辅助脚本
├── docker-compose.yml              # 容器编排配置（数据库、缓存、应用）
├── .env.example                    # 环境变量示例
├── README.md                       # 项目主说明文档
└── LICENSE                         # MIT 许可协议
```

## 贡献指南

我们欢迎并感谢任何形式的贡献，包括但不限于新增资源链接、修复错误、改进文档或提交功能代码。请遵循以下步骤参与贡献：

1.  **查阅现有议题**：访问 GitHub Issues 页面，确认是否已有相关议题讨论您要解决的问题或功能。若没有，请新建议题并详细描述您的想法或发现的问题。
2.  **派生项目并创建分支**：将主仓库派生至您的个人账户，然后克隆派生仓库至本地。请基于 `main` 分支创建一个具有描述性名称的新分支，例如 `feature/add-link-tags` 或 `fix/search-sort-bug`。
3.  **编写代码与测试**：在您的分支上进行修改，并确保代码风格符合项目 ESLint 与 Prettier 配置。对于功能新增或缺陷修复，请编写对应的单元测试或集成测试，确保所有测试用例通过。
4.  **提交变更并推送**：提交信息请遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范，使用 `feat:`、`fix:`、`docs:` 等前缀。完成提交后，将分支推送至您的派生仓库。
5.  **发起拉取请求**：登录 GitHub，从您的派生分支向主仓库的 `main` 分支发起拉取请求。请在 PR 描述中清晰引用相关议题编号，并概述主要变更内容。项目维护者将在约 3 个工作日内进行评审。

## 常见问题

**问：项目是否支持完全离线部署？**

答：除初始克隆和依赖安装外，项目本身在运行时不强制依赖外部互联网。所有外链资源以数据记录形式存储于本地数据库，前端静态资源完全自包含。但外链状态检测功能需要网络访问目标域名，若您部署在内网环境，该功能将受到网络策略限制，但不影响核心的链接管理与检索功能。

**问：如何批量导入现有的书签或收藏夹数据？**

答：项目提供了 `scripts/import-from-html.js` 脚本，可解析 Chrome / Firefox 导出的标准 HTML 书签文件。您也可以将数据整理为符合 `schema.json` 描述的 JSON 数组格式，使用管理后台的“导入”功能或调用 `/api/links/batch-import` 接口完成批量添加。导入前建议使用 `--dry-run` 参数进行验证。

**问：链接状态检测的频率和超时时间是否可配置？**

答：可以。检测任务由后台 Worker 进程调度，相关参数位于 `backend/src/config/scheduler.js` 中，您可以通过环境变量 `CHECK_INTERVAL_CRON`（设置 Cron 表达式）调整检测周期，通过 `CHECK_TIMEOUT_MS` 设置单次请求超时阈值（单位毫秒）。修改后需要重启 Worker 进程生效。

## 许可证

本项目采用 MIT 许可证。您可以自由使用、复制、修改、合并、发布、分发、再许可和/或出售本软件的副本，但需保留原始版权和许可声明。详细条款请参见项目根目录下的 LICENSE 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:53:38
