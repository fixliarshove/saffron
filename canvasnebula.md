# VastLink Navigator

VastLink Navigator 是一个面向技术社区与内容创作者的轻量级外链资源导航与聚合系统。该项目定位于帮助开发者、研究员及内容运营人员高效管理、分类展示和快速检索高频访问的互联网资源链接，尤其适用于需要维护大量外部分发渠道、多媒体内容源或文档站点的团队。通过结构化的数据组织与简洁的 Web 交互界面，本系统解决了资源链接分散、语义不明、更新滞后等常见痛点，可作为中大型项目文档站的补充外链模块，也可独立部署为个人或组织的书签管理工具。

## 功能概览

- **多级分类索引**：支持管理员自定义分类层级，将任意数量的外链按主题、地区、语种或内容类型进行树状归类，便于用户按业务路径浏览。
- **链接状态监测**：内置定时任务，对已收录的 URL 进行可达性与响应码检查，自动标记失效或重定向链接，降低维护成本。
- **批量导入与去重**：支持从 CSV、JSON 或纯文本列表批量导入链接，系统基于域名与完整 URL 双重规则自动去重，并合并已有标签。
- **标签与全文检索**：为每条链接附加自定义标签（如“视频”、“论坛”、“工具”），支持通过标签过滤及对标题、描述、URL 的模糊搜索。
- **访问统计与热度排序**：记录每个链接的被点击次数及最近访问时间，支持按热度或更新时间排序，帮助用户发现活跃资源。
- **只读 API 接口**：提供 RESTful 风格的公共 API，允许第三方系统以 JSON 格式获取全量或分类后的链接数据，便于二次开发。
- **响应式管理面板**：基于 Web 的管理界面适配桌面与移动设备，支持链接的增删改查、分类调整及批量操作，无需命令行干预。

## 应用场景

- **开源项目文档站的外链附录**：为开源社区的 README 或文档站点提供统一的外部依赖、参考文章、视频教程及社区论坛的链接汇总，避免在正文中嵌入大量散乱 URL。
- **媒体资源编辑团队的素材库**：内容生产团队可将经常引用的图片站、字体库、音效平台及版权协议页面集中管理，并通过分类快速定位特定用途的素材源。
- **学术研究中的参考文献网络**：科研人员可批量收录预印本平台、数据存储库、学术搜索引擎及领域权威机构主页，按研究方向或项目阶段分组，便于协作时共享。
- **企业内部知识库的快捷入口**：企业可将常用的运维监控面板、日志系统、代码仓库、设计稿托管及在线协作文档整理为统一导航页，减少员工查找内部工具的时间。

## 快速开始

以下步骤适用于在 Linux 服务器或本地开发环境（需预先安装 Git、Node.js 16+ 及 npm）完成项目的克隆、依赖安装与开发服务启动。

```bash
# 克隆项目仓库至本地
git clone https://github.com/vastlink/navigator.git

# 进入项目工作目录
cd navigator

# 安装所有生产与开发依赖
npm install

# 以开发模式启动本地服务（默认监听端口 3000）
npm run dev
```

启动成功后，访问控制台输出的本地地址（通常为 `http://localhost:3000`）即可进入导航站首页。若需构建生产环境静态文件，请执行 `npm run build`，产物将输出至 `dist` 目录。

## 安装要求

在部署或参与开发之前，请确保运行环境满足以下最低要求。若使用容器化部署，可参考项目 `Dockerfile` 中的基础镜像版本。

| 依赖项 | 必需版本 | 说明 |
| :--- | :--- | :--- |
| Node.js | 16.0.0 或更高 | 运行时环境，用于执行服务端代码及构建脚本 |
| npm | 8.0.0 或更高 | 包管理器，用于安装第三方库及运行脚本 |
| SQLite | 3.35.0 或更高（内置） | 默认轻量级关系型数据库，用于存储链接与分类数据 |
| Git | 2.25.0 或更高 | 版本控制工具，用于克隆仓库及管理补丁 |
| 现代浏览器 | Chrome 90+ / Firefox 88+ | 管理面板及前台页面均基于 ES6 语法与 CSS Grid 布局 |

## 文档导航

为便于不同角色的使用者快速定位所需信息，项目文档已按层面分层组织。下表列出各目录对应的核心内容及其解答的关键问题。

| 层面 | 目录 | 回答的问题 |
| :--- | :--- | :--- |
| 用户指南 | `docs/user-guide/` | 如何浏览分类、检索链接、查看统计信息以及自定义个人首页布局？ |
| 管理手册 | `docs/admin-manual/` | 如何创建分类、批量导入链接、设置监测频率及管理 API 密钥？ |
| 开发参考 | `docs/developer-guide/` | 数据模型如何设计？API 路由如何扩展？前端组件复用规则是什么？ |
| 部署运维 | `docs/deployment/` | 支持哪些部署方式（PM2、Docker、Systemd）？如何配置反向代理与 HTTPS？ |

## 资源列表

本导航项目初始收录的外部资源按内容来源与语种进行分类。以下所有链接均直接来源于用户提供的原始数据，项目本身不修改、不转跳、不代理上述任何地址。各条目按类别分组陈列。

**中文字母与拼音类资源**

- <code>renqishaofuzhongwenzimu.org.cn</code>
- <code>shufurenqizhongwenzimu.org.cn</code>

**视频与影视内容类资源**

- <code>qingqinghebiancaogaoqingmianfei.org.cn</code>
- <code>guguguguoyubanzaixianguankan.org.cn</code>
- <code>guochanjiujiujiu.org.cn</code>

**游戏与娱乐内容类资源**

- <code>guochanzuoshoumi.org.cn</code>
- <code>guochansiwarenyao.org.cn</code>

**特定主题综合类资源**

- <code>mitunjiujiu99jingpinjiujiu.org.cn</code>
- <code>guochanyoucuyoumengyoushuangyouhuang.org.cn</code>
- <code>yazhouxiaoshuoqutupianqu.org.cn</code>

## 项目结构

项目采用模块化分层设计，核心代码位于 `src` 目录，资源与配置文件位于根目录。以下为主要的目录树结构及其职责说明。

```
navigator/
├── src/
│   ├── api/                     # RESTful API 路由定义（链接、分类、统计）
│   ├── core/                    # 核心业务逻辑（链接解析、去重、监测调度）
│   ├── db/                      # 数据库连接池与 SQL 迁移脚本
│   ├── middlewares/             # 请求日志、跨域、鉴权等中间件
│   ├── models/                  # 数据表映射对象（Link, Category, Tag）
│   ├── public/                  # 静态资源（图标、默认图片、robots.txt）
│   ├── services/                # 外部服务集成（邮件告警、Webhook）
│   ├── utils/                   # 通用工具函数（日期格式化、URL 校验）
│   └── views/                   # 服务端渲染的页面模板（管理后台、首页）
├── tests/                       # 单元测试与集成测试用例
├── docs/                        # 完整项目文档（见文档导航章节）
├── scripts/                     # 运维脚本（数据备份、初始化种子数据）
├── .env.example                 # 环境变量配置模板
├── docker-compose.yml           # 容器编排示例（含 SQLite 持久化卷）
├── Dockerfile                   # 基于 Node.js 官方镜像的构建定义
├── package.json                 # 项目元信息与依赖声明
├── README.md                    # 项目入口说明文档（即本文档）
└── vite.config.js               # 前端构建工具配置（若包含 UI 组件）
```

## 贡献指南

我们欢迎社区以多种形式参与本项目的改进。请遵循以下步骤确保贡献过程顺畅。

1.  **问题追踪**：在提交代码前，请先于 GitHub Issues 中搜索是否已有相同或相似的问题报告。若无，则新建 Issue 并清晰描述缺陷、建议或新功能需求，并附上必要的环境信息与复现步骤。
2.  **派生与分支**：将本项目派生（Fork）至个人账户，并基于 `main` 分支创建以 `feature/` 或 `fix/` 为前缀的语义化分支名称，例如 `feature/custom-sort-order`。
3.  **代码与测试**：遵循项目根目录下的 `.eslintrc` 和 `.prettierrc` 配置进行编码。对于新增业务逻辑，请补充对应的单元测试文件至 `tests` 目录，并确保所有现有测试通过。
4.  **提交规范**：提交信息（Commit Message）应使用动词开头的简洁祈使句，如 `Add batch import validator` 或 `Fix cache miss on category update`。若关联 Issue，请在提交信息尾部引用编号。
5.  **发起拉取请求**：将完成开发的分支推送至派生仓库，并向本仓库的 `main` 分支发起拉取请求（Pull Request）。请详细描述变更内容、测试结果以及可能的影响面，等待维护者审阅。

## 常见问题

**问：项目是否支持 MySQL 或 PostgreSQL 作为主数据库？**

答：当前版本默认使用 SQLite 以降低部署门槛，但项目已抽象数据访问层（DAO），您可自行修改 `src/db/connection.js` 中的驱动配置，切换至 MySQL 或 PostgreSQL。需同步调整 `package.json` 中的对应数据库驱动依赖，并更新迁移脚本中的数据类型定义。社区版暂不提供自动迁移工具。

**问：如何导入我已有的大量书签文件（如 Chrome 导出的 HTML）？**

答：本项目未直接提供 HTML 书签解析器，但您可以将书签文件转换为 CSV 格式（列标题映射为 `title`, `url`, `category`），然后通过管理面板的“批量导入”功能上传。或者，您可编写简单脚本调用我们提供的 `POST /api/links/batch` 接口，以 JSON 数组格式发送数据。

**问：链接监测功能会影响页面加载速度吗？**

答：监测任务由独立的后台进程执行（基于 `node-cron` 调度），默认每 24 小时运行一次，且通过异步非阻塞 I/O 发送请求，不会阻塞前台页面的渲染或 API 响应。您可以在 `.env` 文件中调整 `HEALTH_CHECK_CRON` 变量来修改监测频率，或通过 `ENABLE_HEALTH_CHECK=false` 完全关闭该功能。

## 许可证

本项目采用 MIT 许可证进行分发。您被允许自由使用、修改、合并、发布、分发、再许可及销售本软件的副本，但需在软件或文档中包含原始版权声明及许可声明。详见项目根目录下的 `LICENSE` 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:54:36
