# CloudMatch 技术资源导航平台

CloudMatch 是一个面向开发团队与技术决策者的轻量化外链资源汇总系统，专注于收集、分类与展示高价值技术工具、数据服务与行业信息入口。项目定位为纯静态资源聚合层，不对下游数据做任何清洗或加工，仅提供结构化索引与快速跳转能力，适用于技术选型调研、运维监控面板集成、开发环境初始化脚本引用等多种场景。

目标用户包括：需要频繁查阅第三方 API 文档与状态页的后端工程师、负责技术栈评估的架构师、编写基础设施即代码的 DevOps 人员，以及希望建立内部技术雷达的团队管理者。通过将分散在多个域名下的工具入口集中管理，CloudMatch 帮助用户将查找时间从平均 3 至 5 分钟压缩至 10 秒内，显著减少上下文切换损耗。

## 功能概览

- **域名分组收藏** 支持按业务领域（体育数据、实时比分、工具站）对链接进行逻辑分组，每组可独立设置可见性与排序权重。
- **快速模糊检索** 基于标题、描述、标签及 URL 片段的多字段匹配，支持拼音首字母缩写输入，响应时间低于 200 毫秒。
- **批量导入导出** 提供 JSON 与 CSV 两种格式的数据迁移能力，便于团队间共享收藏集或与 CI/CD 流水线集成。
- **可用性被动探测** 在页面加载时异步请求每个链接的 favicon 与响应头，标记异常状态码或超时节点，辅助判断服务健康度。
- **自定义元信息扩展** 每条链接可附加负责人、到期日期、内部评级（P0-P3）及替换建议字段，方便进行生命周期管理。
- **暗色与高对比度主题** 内置三套配色方案，满足低光照环境与视力辅助需求，同时保留操作系统偏好自动跟随能力。
- **键盘全快捷键操作** 无需鼠标即可完成分组切换、收藏、搜索与跳转，适配高频重复操作的效率要求。
- **访问热力图统计** 记录每个链接在本地的点击次数与最后访问时间，数据仅存储于浏览器本地存储，不上传任何服务器。

## 应用场景

- **技术选型调研阶段** 架构团队需要横向对比多个数据服务提供商的接口响应格式与数据新鲜度。CloudMatch 将相关工具站与比分平台归入同一视图，支持同时打开多个目标进行并行评估。
- **运维监控大屏集成** SRE 团队在 Grafana 或自建仪表盘中嵌入 CloudMatch 的只读视图，将关键第三方状态页与备用数据源入口集中放置，故障发生时可在 5 秒内切换至备选链路。
- **新员工开发环境初始化** 技术 Leader 将项目依赖的所有外部资源链接预先导入团队公共分组，新成员克隆仓库后执行一条命令即可拉取最新链接集合，避免逐个询问内网代理或 API 地址。
- **离线文档站镜像参考** 在无外网环境的内部机房内，通过 CloudMatch 导出的 CSV 列表可配合 wget 镜像脚本批量拉取指定域名的静态文档版本，实现受限网络下的知识同步。
- **技术雷达年度更新** 每季度技术委员会评审现有工具链时，使用 CloudMatch 的评级与备注字段快速标记淘汰、保留或升级建议，输出可直接粘贴至 Confluence 的表格内容。

## 快速开始

以下命令将在当前目录下克隆项目仓库，安装 Node.js 依赖，并以开发模式启动本地服务。请确保已安装 Git 与 Node.js 18.x 或更高版本。

```bash
git clone https://github.com/cloudmatch-io/cloudmatch-resources.git
cd cloudmatch-resources
npm install
npm run dev
```

启动成功后，访问控制台输出的本地地址（通常为 http://localhost:5173）即可看到默认分组视图。首次加载时会自动生成示例数据，包含本仓库收录的全部原始链接。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 18.x 或 20.x LTS | 运行时环境，用于执行构建脚本与开发服务器 |
| npm | 9.x 或更高 | 包管理器，用于安装项目依赖 |
| Git | 2.25 或更高 | 版本控制工具，用于克隆仓库与提交更新 |
| 现代浏览器 | Chrome 110+ / Firefox 109+ / Edge 110+ | 支持 ES2022 语法与 IndexedDB 存储 |
| 磁盘空间 | 至少 200 MB | 包含源代码、依赖包及构建缓存 |
| 网络访问 | 外网可达（首次构建时） | 用于下载 npm 包及加载在线字体资源 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | /docs/user-guide/ | 如何自定义分组、导入导出数据、使用键盘快捷键？ |
| 配置参考 | /docs/config/ | 支持哪些元信息字段？排序与过滤规则如何调整？ |
| 开发指南 | /docs/development/ | 如何扩展新解析器？如何替换默认存储引擎？ |
| 部署手册 | /docs/deployment/ | 支持哪些静态托管平台？环境变量有哪些作用？ |
| 设计决策 | /docs/adr/ | 为什么选择本地存储而非云端？为何不提供搜索建议？ |
| 故障排除 | /docs/troubleshooting/ | 图标无法加载、导入数据失败时如何处理？ |

## 资源列表

### 实时比分与数据聚合类

<code>qiutanbifen888.org.cn</code>

<code>tiqiuwang.org.cn</code>

<code>lanqiubifennbanba.org.cn</code>

<code>zuqiujishibifena.org.cn</code>

<code>zuqiujishibifenb.org.cn</code>

<code>zuqiujishibifenc.org.cn</code>

<code>tiqiuwanga.org.cn</code>

<code>tiqiuwangb.org.cn</code>

<code>tiqiuwangc.org.cn</code>

<code>qiutanzuqiubifena.org.cn</code>

## 项目结构

```
cloudmatch-resources/
├── index.html                 # 主入口页面，包含根 DOM 与全局样式加载
├── package.json               # 项目元信息与依赖版本锁定
├── vite.config.js             # 构建工具配置，含代理与路径别名
├── .env.example               # 环境变量模板，用于自定义标题与默认分组
├── public/                    # 静态资源目录，不经构建直接复制
│   ├── favicon.ico            # 网站图标
│   └── robots.txt             # 爬虫策略文件，禁止索引所有路径
├── src/                       # 源代码主目录
│   ├── main.js                # 应用启动入口，初始化路由与状态
│   ├── App.vue                # 根组件，定义整体布局与主题切换逻辑
│   ├── store/                 # Pinia 状态管理模块
│   │   ├── links.js           # 链接集合的增删改查与排序逻辑
│   │   ├── groups.js          # 分组结构与可见性控制
│   │   └── settings.js        # 用户偏好设置（主题、语言、默认视图）
│   ├── components/            # 可复用 UI 组件
│   │   ├── SearchBar.vue      # 搜索输入框与过滤状态展示
│   │   ├── LinkCard.vue       # 单个链接卡片，显示标题、描述、状态标记
│   │   ├── GroupPanel.vue     # 分组侧边栏，支持拖拽排序
│   │   └── ImportExport.vue   # 批量数据导入导出对话框
│   ├── composables/           # 组合式函数，封装业务逻辑钩子
│   │   ├── useProbe.js        # 可用性探测的并发控制与缓存策略
│   │   └── useKeyboard.js     # 全局键盘快捷键映射与防冲突处理
│   ├── utils/                 # 通用工具函数
│   │   ├── parser.js          # 从 URL 提取域名、标题与 favicon 地址
│   │   ├── storage.js         # IndexedDB 的读写封装，含版本迁移
│   │   └── validator.js       # 链接格式校验与重复检测
│   └── assets/                # 样式、字体与静态图资源
│       ├── main.css           # 全局样式变量与基础重置
│       └── themes/            # 暗色、高对比度及色盲友好主题
├── tests/                     # 单元测试与集成测试脚本
│   ├── unit/                  # Vitest 单元测试，覆盖核心工具函数
│   └── e2e/                   # Playwright 端到端测试，模拟用户操作
├── docs/                      # 完整文档源文件，使用 Markdown 编写
│   ├── user-guide/            # 面向最终用户的操作说明
│   ├── development/           # 面向贡献者的架构与接口文档
│   └── adr/                   # 架构决策记录，含时间戳与背景说明
└── scripts/                   # 辅助脚本，用于数据迁移与文档生成
    ├── seed.js                # 生成初始示例数据，包含全部默认链接
    └── build-docs.js          # 将 Markdown 文档转换为静态 HTML 页面
```

## 贡献指南

1. **Fork 仓库并创建特性分支** 从主仓库 Fork 到个人账号下，然后使用 `git checkout -b feature/your-feature-name` 创建新分支，避免直接在主分支上修改。

2. **安装依赖并启动开发服务器** 在项目根目录执行 `npm install` 安装全部依赖，之后运行 `npm run dev` 启动开发环境。确保所有修改都能在本地预览中正常显示。

3. **编写或修改代码时遵循现有代码风格** 项目使用 ESLint 与 Prettier 进行格式化，提交前请运行 `npm run lint` 与 `npm run format`。新增功能需同步添加对应的单元测试用例。

4. **更新相关文档与示例数据** 若修改了配置文件结构或新增环境变量，必须同步更新 `/docs` 下的对应章节。若新增了示例链接，请同时修改 `scripts/seed.js` 中的初始数据。

5. **提交 Pull Request 并描述变更内容** 推送到远程分支后，在 GitHub 上创建 PR，标题需简洁概括变更，描述部分列出测试结果、影响范围及是否破坏向后兼容性。等待至少一名维护者审核后合并。

## 常见问题

**Q: 为什么我导入的 CSV 文件提示格式错误？**

A: 请确保 CSV 文件使用 UTF-8 编码，且表头包含 `title`、`url`、`description`、`groupId` 四列。若从 Excel 导出，请另存为「CSV UTF-8」格式而非普通 CSV。项目内置的导出功能会生成符合要求的模板文件，建议先导出一份空模板再填写数据。

**Q: 可用性探测显示某些链接为异常，但我手动访问正常，如何处理？**

A: 被动探测依赖于浏览器同源策略与跨域资源共享头，部分服务器可能拒绝来自本地 origin 的预检请求。您可以在设置中关闭自动探测，或为特定域名添加白名单。另外，探测超时阈值默认为 5000 毫秒，网络较慢时可调整 `settings.js` 中的 `probeTimeout` 参数。

**Q: 项目数据完全存储在本地，如果我更换浏览器或清理缓存，数据会丢失吗？**

A: 是的，所有链接与配置仅保存在您当前浏览器的 IndexedDB 中。我们强烈建议定期使用「导出数据」功能将快照保存为 JSON 文件，或配置自动导出到 WebDAV 等外部存储。后续版本将提供基于 Git 仓库的云同步插件，但当前版本不涉及任何服务端存储。

## 许可证

MIT

> 外链数量: 10 | 生成时间: 2026-08-11 16:53:38
