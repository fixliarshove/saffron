# Rihan Resource Hub

Rihan Resource Hub 是一个面向中文互联网技术内容聚合与导航的开源项目，致力于系统化收集、分类和呈现特定垂直领域内的公开可访问网络资源。本项目定位为技术研究辅助工具，适用于需要快速检索、批量访问和结构化管理大量外链资源的开发者、数据分析师及内容研究人员。通过提供清晰的资源分类体系、标准化的项目文档以及自动化的链接校验机制，本项目有效解决了分散资源难以维护、链接失效难以追踪、分类混乱难以复用等核心问题。

---

## 功能概览

- **多层级资源分类体系**：按照内容主题、文件类型、更新频率等维度对收录的 URL 进行精细化标签管理，支持自定义分类规则。
- **批量链接可用性检测**：内置链接状态检查脚本，可定时探测各资源节点的响应码与访问延迟，自动标记异常链接。
- **结构化元数据维护**：为每条资源记录关联描述字段、收录日期、来源备注等信息，所有元数据以纯文本形式存储于项目仓库。
- **静态文档自动生成**：基于项目内的资源清单和分类配置，自动生成 Markdown 格式的导航页面，方便快速浏览和分享。
- **变更历史追踪**：所有资源增删改操作均通过 Git 提交记录进行版本管理，支持回溯任意历史状态的完整资源列表。
- **按场景筛选视图**：提供按使用场景（如数据采集、内容分析、存档查阅）过滤资源的预定义视图，提升检索效率。
- **私有化部署支持**：项目结构完全基于静态文件，可轻松部署至任意 HTTP 服务器或对象存储服务，无需数据库依赖。
- **社区贡献模板**：提供标准化的资源提交流程和模板，确保外部贡献的资源信息格式统一、字段完整。

---

## 应用场景

- **数据采集任务的前期调研**：研究人员在启动特定领域的数据采集项目前，可通过本项目的分类索引快速发现相关数据源，评估资源覆盖度，避免重复造轮子。
- **内容运营的渠道监控**：内容运营人员可利用本项目的链接清单作为种子集合，配合第三方监控工具定期检查竞品或行业相关页面的更新动态，辅助内容选题决策。
- **互联网档案馆的补充索引**：对于部分历史内容或更新频率较低的页面，本项目维护的静态链接列表可作为 Internet Archive 等回溯服务之外的补充导航路径。
- **技术文档中的参考资源附录**：开发者在撰写技术报告或项目文档时，可直接引用本项目中的资源分类结构和部分稳定链接，作为附录参考材料的来源之一。
- **个人知识库的外链备份**：个人用户可将本项目的资源清单导入到本地笔记软件或书签管理工具中，形成一份结构化的外部知识索引，降低对单一浏览器的依赖。

---

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境。请确保系统已安装 Git 和 Node.js（版本 16.x 或更高）。

```bash
# 克隆项目仓库
git clone https://github.com/rihan-resource/rihan-hub.git
cd rihan-hub

# 安装依赖（用于链接检测和文档生成脚本）
npm install

# 运行本地静态站点生成与预览
npm run build
npm run serve
```

执行完成后，打开浏览器访问 `http://localhost:8080` 即可查看生成的导航页面。资源列表默认位于项目根目录的 `resources.json` 文件中，您可以手动编辑该文件或通过 `npm run add` 交互式命令添加新资源。

---

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 16.x 或更高 | 用于运行资源校验、文档生成及本地服务脚本 |
| npm | 8.x 或更高 | 依赖管理工具，随 Node.js 一同安装 |
| Git | 2.30 或更高 | 用于克隆仓库、提交变更和版本回溯 |
| 网络连通性 | 能够访问公网 | 链接检测功能需要对外部域名发起 HTTP 请求 |
| 文件系统权限 | 读写权限 | 项目需要创建临时缓存文件和输出目录 |
| 可选：Docker | 20.x 或更高 | 若使用容器化部署方式，则需 Docker 环境 |
| 可选：Python 3 | 3.9 或更高 | 部分扩展校验脚本（如 SSL 证书检查）依赖 Python |

---

## 文档导航

| 层面 | 目录/文件 | 回答的问题 |
|------|-----------|------------|
| 用户指南 | `docs/user-guide.md` | 如何使用本项目查找资源、自定义分类视图以及配置本地预览？ |
| 维护手册 | `docs/maintainer-guide.md` | 如何新增资源、更新失效链接、处理社区提交的合并请求？ |
| 开发参考 | `docs/developer-guide.md` | 资源 JSON Schema 定义、脚本 API 说明以及如何扩展检测插件？ |
| 部署说明 | `docs/deployment.md` | 如何将生成的静态站点部署到 Nginx、S3 或 Cloudflare Pages？ |
| 常见问题 | `docs/faq.md` | 链接检测出现超时如何处理？资源分类标准是什么？是否支持全文搜索？ |
| 变更日志 | `CHANGELOG.md` | 每个版本新增了哪些资源分类、修复了哪些脚本缺陷、更新了哪些依赖？ |

---

## 资源列表

### 核心资源组（主分类）

- <code>rihanrenqixilie.org.cn</code>
- <code>shibajinzaixianmianfeiguankan.org.cn</code>
- <code>shunvtiantang.org.cn</code>
- <code>yazhoupapa.org.cn</code>

### 辅助资源组（子分类扩展）

- <code>yeyejiujiu.org.cn</code>
- <code>oumeizipaiqu.org.cn</code>
- <code>wuyeneishe.org.cn</code>

### 补充资源组（专项归档）

- <code>jiujiujiujiuguochan.org.cn</code>
- <code>renqixiliezhongwenzimu.org.cn</code>
- <code>neishemama.org.cn</code>

---

## 项目结构

```
rihan-hub/
├── .github/                         # GitHub 社区模板与 CI 配置
│   └── workflows/
│       └── link-check.yml           # 定时执行链接可用性检测的 GitHub Actions 工作流
├── scripts/                         # 核心脚本目录
│   ├── validate.js                  # 校验 resources.json 的 JSON Schema 完整性
│   ├── checker.js                   # 并发执行 HTTP 探测，输出状态报告
│   └── generator.js                 # 根据分类配置生成静态 Markdown/HTML 导航页
├── src/
│   ├── data/
│   │   ├── resources.json           # 主资源清单（包含全部 URL 及其元数据）
│   │   └── categories.json          # 分类体系定义（标签、别名、层级关系）
│   ├── templates/                   # 文档生成所使用的模板文件
│   │   ├── header.md
│   │   ├── footer.md
│   │   └── resource-item.md
│   └── assets/                      # 静态资源（Logo、样式等）
│       └── style.css
├── docs/                            # 生成的用户文档
│   ├── index.md                     # 入口导航页
│   ├── user-guide.md
│   ├── maintainer-guide.md
│   └── developer-guide.md
├── tests/                           # 单元测试与集成测试脚本
│   ├── validate.test.js
│   └── checker.test.js
├── .gitignore
├── package.json                     # npm 依赖与脚本命令定义
├── README.md                        # 项目主说明文档（本文件）
├── CHANGELOG.md                     # 版本变更历史
└── LICENSE                          # MIT 许可证全文
```

---

## 贡献指南

1. 在 GitHub 上 Fork 本仓库，并克隆到本地开发环境。请确保您的 Fork 与主仓库保持同步，避免提交冲突。
2. 在 `src/data/resources.json` 中添加或修改资源条目。每个条目必须包含 `url`、`title`、`category` 和 `description` 字段，并遵循 `schema/resources.schema.json` 中定义的格式规范。
3. 在本地运行 `npm run test` 执行全部校验脚本，包括 JSON 格式校验、链接可访问性检测（仅对新增或修改的条目）以及分类标签合法性检查。所有测试必须通过方可提交。
4. 提交变更时请使用清晰的提交信息，格式建议为 `[类型] 简短描述`，例如 `[add] 新增两组技术博客资源` 或 `[fix] 更正拼音分类标签命名`。提交后推送到您的 Fork 仓库。
5. 通过 GitHub 界面创建 Pull Request，并在描述中说明变更的背景、影响范围以及是否涉及破坏性改动。项目维护者将在 3 个工作日内进行审查和合并。

---

## 常见问题

**问：检测脚本报告某个链接超时，但我在浏览器中可以正常访问，是什么原因？**

答：常见原因包括：服务器对无 `User-Agent` 头或特定 `User-Agent` 的请求返回非标准响应；网络环境存在防火墙规则，拦截了脚本发起的大规模探测请求；目标站点启用了 CDN 或 DDoS 防护策略，对高频 IP 进行限制。建议首先检查脚本的请求头配置，可尝试设置 `User-Agent` 为常见浏览器值；其次，可在 `config.js` 中调整并发数和超时阈值，降低探测速率。若问题持续，可将该 URL 加入检测白名单，手动标记为可访问。

**问：项目的资源分类标准是什么？我可以建议新增分类标签吗？**

答：目前采用两级分类体系：一级按内容领域（如 `technology`、`culture`、`education`），二级按文件类型或用途（如 `documentation`、`video`、`forum`）。该分类基于常见互联网资源形态设计，旨在平衡分类粒度和维护成本。如果您有新的分类建议，请先在 `docs/faq.md` 中查看是否已有相关讨论，若无，可提交一个 Issue 说明新分类的名称、定义、适用资源样例以及预期使用频率。核心维护者会根据合理性和通用性评估是否采纳。

**问：如何保持本地资源和远程文档的一致性？**

答：项目使用 `npm run build` 命令统一生成所有静态文档。该命令会读取 `resources.json` 和 `categories.json` 中的数据，按照 `templates/` 目录下的模板渲染生成完整的 Markdown 页面。因此，您只需维护数据源文件，无需手动编辑 `docs/` 目录下的输出文件。所有通过 Pull Request 合并的变更都会触发 CI 重新构建，确保远程仓库的文档始终与资源清单同步。

---

## 许可证

本项目采用 MIT 许可证进行开源。您可以在遵守许可证条款的前提下自由使用、修改、复制和分发本项目的代码及文档。完整的许可证文本请参阅项目根目录下的 `LICENSE` 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:54:34
