# OneNav Bookmark Hub

OneNav Bookmark Hub 是一个面向技术团队、内容创作者和个人知识管理者的开源外链资源聚合与导航系统。项目定位为轻量级、可自托管的书签与资源导航门户，专注于解决信息碎片化时代下，高质量垂直领域资源难以统一归类、快速检索和团队共享的问题。通过结构化的资源组织方式，帮助用户在特定技术领域或兴趣方向建立可维护、可扩展的知识检索起点。

本项目并非通用收藏夹工具，而是围绕特定主题（如中文内容生态、特定行业资源、技术社区入口等）构建的垂直导航站点。适用于内部知识库嵌入、项目文档配套资源墙、个人浏览器起始页替代方案等场景。所有资源以对外链形式存储与管理，支持标签过滤、全文搜索及访问状态监控，确保资源长期可用。

## 功能概览

- **分级资源目录**：支持无限层级分类，可按照技术栈、业务场景、内容形态等维度组织外链资源，便于结构化浏览。

- **多模式快速检索**：内置标题模糊匹配、标签精确筛选、域名反向查询三种检索模式，配合键盘快捷键操作，提升高频访问效率。

- **资源可用性探活**：后台定时发起 HTTP HEAD 请求检测外链可达性，异常状态在界面以视觉标记警示，辅助管理员及时清理失效资源。

- **导入与导出兼容**：支持 HTML 书签文件（Netscape 格式）批量导入，以及 JSON、CSV 格式的全量导出，便于与其他导航工具或浏览器无缝迁移。

- **团队协作注解**：每条资源条目可附加公开评论与多标签标注，支持团队成员间共享上下文信息，避免重复发现与理解成本。

- **访问热度统计**：基于点击日志自动计算资源热度趋势，提供近 7 天、30 天两个时间窗口的访问排行，辅助内容推荐决策。

- **响应式布局与主题切换**：默认提供浅色/深色两套主题，适配桌面与移动端屏幕尺寸，满足不同使用环境下的视觉舒适度需求。

## 应用场景

- **内部技术文档配套资源墙**：研发团队可在项目文档中嵌入 OneNav 实例，集中存放依赖镜像源、官方 SDK 下载页、API 参考文档、社区论坛等关键外链，避免成员在分散的浏览器收藏夹中反复查找。

- **垂直领域知识库起始页**：研究人员或内容运营人员可针对特定主题（如中文开源项目、特定语料库资源、区域互联网服务商列表）构建专属导航页，作为深入调研的固定入口，提升信息收集阶段的效率。

- **个人浏览器起始页替代方案**：普通用户可将自部署实例设置为浏览器新标签页，按工作、学习、娱乐等维度分类日常高频访问站点，并结合访问热度统计定期清理低频或失效链接。

- **社区贡献资源聚合展示**：开源社区或线上兴趣小组可利用该项目搭建共享资源看板，所有成员均可提交新链接，由维护者审核上架，形成群体维护的动态资源目录。

## 快速开始

以下步骤适用于 Linux / macOS 环境，Windows 用户建议通过 WSL2 或 Git Bash 执行。

```bash
# 1. 克隆项目仓库
git clone https://github.com/onenavhub/bookmark-hub.git
cd bookmark-hub

# 2. 安装依赖（使用 pnpm，若未安装请先执行 npm install -g pnpm）
pnpm install

# 3. 初始化本地配置文件
cp .env.example .env.local

# 4. 启动开发服务器（默认监听 http://localhost:3000）
pnpm run dev
```

生产环境部署请参考下一章节的依赖要求，并使用 `pnpm run build` 构建静态产物，配合 Nginx 或 Caddy 托管 `dist` 目录。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 18.18.0 或更高 | 运行时环境，推荐使用 LTS 版本 |
| pnpm | 8.0.0 或更高 | 包管理器，用于依赖安装与脚本执行 |
| SQLite | 3.35.0 或更高（内置） | 默认嵌入式数据库，无需额外安装 |
| Git | 2.25.0 或更高 | 用于克隆仓库和版本管理 |
| 现代浏览器 | Chrome 90+ / Firefox 88+ / Edge 90+ | 管理后台前端运行环境 |
| 磁盘空间 | 至少 200 MB | 包含源代码、依赖包及 SQLite 数据文件 |
| 内存 | 最低 512 MB，推荐 1 GB | 开发模式额外需要约 300 MB 用于热重载 |
| 网络 | 可访问外网 | 用于首次依赖安装及外链可用性探测 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 入门指南 | `/docs/getting-started.md` | 如何完成首次部署？如何进行初始配置？默认管理员账号是什么？ |
| 使用手册 | `/docs/user-guide.md` | 如何添加、编辑、删除资源链接？如何批量导入/导出书签？如何自定义分类？ |
| 运维参考 | `/docs/operations.md` | 如何执行资源探活任务？如何备份与恢复 SQLite 数据？如何迁移至 PostgreSQL？ |
| 扩展开发 | `/docs/development.md` | 如何开发自定义主题？如何增加新的检索过滤器？如何通过 REST API 管理资源？ |
| 常见配置 | `/docs/configuration.md` | 环境变量文件支持哪些参数？如何修改站点标题、Logo 和页脚信息？ |
| 版本发布 | `/docs/releases.md` | 当前版本号是多少？历史版本有哪些破坏性变更？升级前需要执行哪些数据迁移？ |

## 资源列表

本项目初始资源库聚焦于中文互联网特定领域的公开资源站点，所有链接均来自社区贡献与公开渠道汇总。以下列表按主题类别组织，便于按需浏览。

### 中文综合资源类

- <code>zhongwenzimurenqisiwa.org.cn</code>
- <code>zhongwenzimuyiersan.org.cn</code>
- <code>yazhouzhongwenzimuyiqu.org.cn</code>

### 特定内容主题类

- <code>baoruwuma.org.cn</code>
- <code>wuyeguochan.org.cn</code>

### 社区热点与交互类

- <code>renqidaxiangjiao.org.cn</code>
- <code>bukarenqi.org.cn</code>
- <code>renrenqirenrenai.org.cn</code>

### 内容更新频率类

- <code>tiantianganyeyeqi.org.cn</code>

### 地域与语言相关类

- <code>yazhouhenhenai.org.cn</code>

## 项目结构

```
bookmark-hub/
├── .github/                         # GitHub 工作流配置
│   └── workflows/
│       ├── ci.yml                   # 持续集成：单元测试与构建检查
│       └── release.yml              # 自动发布：打标签与生成 release notes
├── apps/
│   ├── web/                         # 主应用前端（Next.js 14 + React 18）
│   │   ├── src/
│   │   │   ├── app/                 # App Router 页面路由
│   │   │   ├── components/          # 可复用 UI 组件（分类树、搜索栏、资源卡片）
│   │   │   ├── hooks/               # 自定义 React Hooks（鉴权、筛选、探活）
│   │   │   ├── lib/                 # 工具函数（请求封装、数据校验）
│   │   │   └── styles/              # 全局样式与主题变量
│   │   └── package.json
│   └── api/                         # RESTful API 服务（Express + SQLite）
│       ├── src/
│       │   ├── routes/              # 路由定义（资源 CRUD、分类管理、导入导出）
│       │   ├── controllers/         # 业务逻辑控制器
│       │   ├── models/              # 数据模型层（SQL 映射与迁移）
│       │   ├── middleware/          # 鉴权、日志、错误处理中间件
│       │   └── workers/             # 后台任务（探活检测、统计聚合）
│       └── package.json
├── packages/
│   ├── shared-types/                # 前后端共享 TypeScript 类型定义
│   ├── ui-lib/                      # 内部 UI 组件库（按钮、对话框、表格）
│   └── config-eslint/               # 统一 ESLint + Prettier 配置
├── docs/                            # 完整文档（见上一章节文档导航）
├── scripts/                         # 运维与辅助脚本（数据迁移、种子填充）
├── data/                            # 持久化数据目录
│   ├── sqlite.db                    # 默认 SQLite 数据库文件
│   └── backups/                     # 自动备份存档
├── tests/                           # 单元测试与集成测试
│   ├── unit/                        # 后端模型与工具函数测试
│   └── e2e/                         # 前端端到端测试（Playwright）
├── docker-compose.yml               # 容器化编排（含反向代理与 SSL 示例）
├── Dockerfile                       # 多阶段构建镜像文件
├── .env.example                     # 环境变量配置模板
├── pnpm-workspace.yaml              # pnpm 工作区定义
└── README.md                        # 项目介绍与快速入口（本文件）
```

## 贡献指南

我们欢迎各类形式的贡献，包括但不限于新增资源链接、完善文档、修复缺陷与性能优化。请遵循以下流程以确保协作顺畅。

1. **提交资源推荐**：通过 Issue 模板提交新增外链申请，需附带资源简介、分类建议以及至少一条访问验证记录。新增资源将经过社区投票或维护者审核后合并至默认列表。

2. **代码与文档变更**：Fork 本项目，在 `main` 分支基础上创建特性分支（命名格式 `feat/描述` 或 `fix/描述`），完成后提交 Pull Request。请确保所有现有单元测试通过，并补充新功能的对应测试用例。

3. **本地验证流程**：运行 `pnpm run test` 执行全部测试套件，使用 `pnpm run lint` 检查代码风格。若涉及数据库 Schema 变更，请提供对应的迁移脚本（位于 `apps/api/src/models/migrations`）。

4. **反馈与问题报告**：使用 GitHub Issues 报告 Bug 或提出改进建议。请填写完整环境信息（Node 版本、浏览器版本、部署方式），并附带可复现的最小步骤或截图。

5. **翻译与本地化**：若希望增加其他语言的界面文案或文档翻译，请参考 `apps/web/src/locales` 下的 i18n 配置文件，提交对应的 JSON 键值对。

## 常见问题

**问：首次启动后没有默认分类与资源数据，如何快速填充示例内容？**

答：项目根目录下提供了种子数据脚本。执行 `pnpm run seed` 即可自动创建初始分类（包括示例中的各类别）并导入资源列表中的部分占位链接。该脚本不会覆盖已有数据，仅当数据库为空时生效。你也可以通过管理后台的“批量导入”功能上传自定义书签 HTML 文件进行初始化。

**问：外链可用性检测任务报告大量超时或失败，但浏览器中访问实际可用，如何调整？**

答：探活任务默认超时时间为 3000 毫秒，且不跟随重定向。若目标站点响应较慢或采用严格反爬策略，请在 `.env.local` 中调整 `HEALTH_CHECK_TIMEOUT` 与 `HEALTH_CHECK_USER_AGENT` 变量。同时，建议将检测间隔从默认的 6 小时延长至 24 小时，以避免被目标服务器限流。详细调优参数请参考运维文档中的探活配置章节。

**问：能否将 SQLite 替换为 MySQL 或 PostgreSQL 以支持高并发读写？**

答：可以。项目数据访问层使用 Knex.js 构建，支持多数据库方言。请参考 `docs/operations.md` 中的数据库迁移指南，修改 `DB_CLIENT`、`DB_CONNECTION_STRING` 等环境变量，并运行 `pnpm run migrate:prod` 执行 Schema 同步。注意，不同数据库的日期函数与全文索引实现略有差异，生产切换前建议在预发布环境充分测试。

## 许可证

本项目采用 MIT 许可证。任何个人或组织均可自由使用、修改、分发本软件，包括用于商业目的。完整版权声明与免责条款请参阅仓库根目录下的 `LICENSE` 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:53:38
