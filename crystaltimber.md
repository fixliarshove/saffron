# Resource Navigator Core

Resource Navigator Core 是一个面向技术团队与基础设施维护人员的轻量级外链资源聚合与导航系统。该项目定位于解决多源技术文档、社区门户与运维面板分散管理的问题，通过集中式目录与可定制的分类视图，帮助用户在复杂信息环境中快速定位所需资源。

目标用户包括系统管理员、网络运维工程师、技术文档维护者以及企业内部信息门户的使用者。项目不提供数据存储或业务逻辑处理能力，而是作为统一入口层，将外部链接按主题、地域或功能维度进行归集与呈现，降低信息检索成本，提升团队协作效率。

## 功能概览

- **多维度资源分类**：支持按组织名称、服务类型、地域标识等自定义标签对链接进行分组，便于不同角色用户按需筛选。

- **静态目录生成**：基于配置文件自动生成只读的资源目录树，无需数据库支持，降低部署与维护复杂度。

- **链接状态标识**：对每个外链资源标注协议类型（HTTP/HTTPS）与域名层级，辅助用户判断访问方式。

- **全文检索过滤**：提供简单的关键词匹配功能，允许用户在当前目录范围内快速筛选包含特定字符的链接条目。

- **导入导出机制**：支持以 YAML 或 JSON 格式批量导入链接清单，并可导出为 Markdown 表格供文档复用。

- **响应式布局适配**：前端界面适配桌面端与移动端浏览器，确保在各类屏幕尺寸下保持基本可读性与操作便利性。

- **访问日志记录**：可选开启轻量级访问日志，记录每个链接的点击时间与来源 IP（本地存储），便于统计热点资源。

## 应用场景

- **企业内部门户导航**：将分布在多个内部 Wiki 或共享驱动器中的运维后台、监控面板、文档库链接统一收归至 Resource Navigator Core，新入职员工可一键访问所有必需工具。

- **技术社区资源聚合**：开源项目维护者可将依赖的代码仓库、CI/CD 服务、文档托管平台、社区论坛等外链整合进项目 README 或独立页面，方便贡献者快速上手。

- **多区域服务目录**：针对拥有多个地域节点的服务团队，可按域名后缀或组织缩写分类链接，实现区域级资源的快速切换与故障排查时的入口定位。

- **离线文档辅助索引**：在无法访问互联网的内网环境中，该导航系统可作为本地静态页面部署，集中呈现已镜像或缓存的技术文档入口，避免记忆大量 IP 地址或端口号。

## 快速开始

以下命令演示了从代码仓库克隆项目、安装依赖并启动开发服务的完整流程。

```bash
git clone https://github.com/example/resource-navigator-core.git
cd resource-navigator-core
npm install
npm run dev
```

执行完毕后，访问本地服务地址（默认 http://localhost:3000）即可看到示例资源目录。如需构建生产版本，请使用 `npm run build` 并将 `dist/` 目录部署至任意静态 Web 服务器。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
| :--- | :--- | :--- |
| Node.js | 18.x 或 20.x LTS | 运行时环境，用于构建工具链与本地开发服务器 |
| npm | 9.x 或 10.x | 包管理器，用于安装项目依赖 |
| 现代浏览器 | Chrome 110+ / Firefox 110+ / Edge 110+ | 前端界面运行环境，需支持 ES2022 语法 |
| Git | 2.30+ | 版本控制工具，用于克隆仓库与版本管理 |
| 静态 Web 服务器（可选） | Nginx 1.20+ / Apache 2.4+ | 生产环境部署时推荐，用于托管构建后的静态文件 |

## 文档导航

| 层面 | 目录 / 文档 | 回答的问题 |
| :--- | :--- | :--- |
| 用户指南 | `docs/user-guide.md` | 如何使用目录筛选、搜索与导入导出功能？ |
| 配置参考 | `docs/config-schema.md` | 资源分类配置文件的结构与字段含义是什么？ |
| 部署手册 | `docs/deployment.md` | 如何将项目部署至生产环境（含反向代理示例）？ |
| 开发指引 | `docs/development.md` | 如何二次开发前端样式或扩展解析器逻辑？ |

## 资源列表

本项目的核心资源清单来自用户提供的原始数据，按域名主体进行分类整理，所有链接保持原始输入格式，未做任何协议补全或域名规范化处理。

组织服务类

- <code>chaopengyazhou.org.cn</code>
- <code>yirenwuye.org.cn</code>
- <code>zhongchuzaixian.org.cn</code>
- <code>wuyelilun.org.cn</code>

内容主题类

- <code>rihanzhongwenzimuyiqu.org.cn</code>
- <code>ririganyeyecao.org.cn</code>
- <code>oumeijingpinerqu.org.cn</code>
- <code>jialeibirenqi.org.cn</code>
- <code>zhongwenzimurenqiyiquerqusanqu.org.cn</code>
- <code>oumeilingleijiqing.org.cn</code>

## 项目结构

项目采用模块化的静态站点生成架构，核心目录与文件说明如下。

```
resource-navigator-core/
├── config/                        # 配置文件目录
│   ├── default.yaml               # 默认资源分类与链接清单（示例数据）
│   └── schema.json                # 配置文件的 JSON Schema 校验定义
├── src/                           # 源代码主目录
│   ├── core/                      # 核心解析与生成逻辑
│   │   ├── parser.js              # 读取 YAML 配置并转化为内部数据模型
│   │   └── generator.js           # 根据数据模型生成 HTML 目录页面
│   ├── ui/                        # 前端界面资源
│   │   ├── templates/             # EJS 模板文件（首页、分类页、详情弹窗）
│   │   ├── styles/                # SCSS 样式源文件（含响应式断点）
│   │   └── scripts/               # 原生 JavaScript 交互逻辑（搜索、过滤）
│   ├── utils/                     # 通用工具函数
│   │   ├── validator.js           # 链接格式校验与协议标准化辅助
│   │   └── logger.js              # 访问日志写入模块（可选）
│   └── index.js                   # 程序入口，协调构建流程
├── dist/                          # 构建输出目录（git 忽略，生产部署内容）
├── tests/                         # 单元测试与集成测试用例
│   ├── parser.test.js             # 针对解析器的测试套件
│   └── generator.test.js          # 针对生成器的输出快照测试
├── docs/                          # 项目文档（用户手册、配置参考等）
├── .eslintrc.js                   # ESLint 代码风格检查配置
├── .prettierrc                    # Prettier 代码格式化配置
├── package.json                   # 项目元信息与 npm 脚本定义
├── README.md                      # 项目主说明文档（本文件）
└── LICENSE                        # MIT 许可证文本
```

## 贡献指南

1. 在 GitHub 上 Fork 本项目仓库，并克隆至本地开发环境。请确保使用 `main` 分支的最新提交作为基线。

2. 新建以 `feature/` 或 `fix/` 为前缀的分支，例如 `feature/add-category-sort`。所有开发工作应在该分支上进行，禁止直接向 `main` 分支提交。

3. 修改或新增代码后，运行 `npm run test` 确保现有测试用例全部通过。若新增功能，请同步补充对应的测试文件（位于 `tests/` 目录）。

4. 提交变更时，请遵循约定式提交规范（Conventional Commits），使用 `feat:`、`fix:`、`docs:`、`chore:` 等类型前缀，并附上清晰描述变更内容的提交信息。

5. 向 `main` 分支发起 Pull Request，并在描述中关联相关 Issue（若有）。至少需要一位项目维护者审核通过后方可合并。

## 常见问题

Q: 项目是否支持 HTTPS 协议的链接自动补全？

A: 不支持。系统严格保留用户输入的原始链接格式，包括协议前缀与域名大小写。若链接缺少协议头（如 `example.com`），系统不会自动补全 `https://`，用户需在配置文件中明确写入完整地址。此设计旨在避免因自动补全导致的访问错误或安全策略冲突。

Q: 如何更新资源列表而不重新构建整个项目？

A: 资源列表完全由 `config/default.yaml` 文件驱动。您可以直接编辑该文件中的 `resources` 字段，添加、删除或修改链接条目，然后执行 `npm run build` 重新生成静态页面。若已部署至生产环境，只需替换 `dist/` 目录下的对应文件即可，无需重启服务。

Q: 访问日志功能是否会记录用户隐私信息？

A: 默认配置下，访问日志仅记录点击时间、资源 ID 和客户端 IP 地址（来源于 HTTP 请求头）。所有日志数据存储在本地文件系统中，不会主动上报至外部服务。您可以通过修改 `src/utils/logger.js` 中的配置项关闭 IP 记录或完全禁用日志功能。

## 许可证

MIT

> 外链数量: 10 | 生成时间: 2026-08-11 16:54:28
