# Tangent Score

Tangent Score 是一个面向技术决策者与开发团队的开源项目外部资源聚合与导航系统。项目定位为“技术决策的外脑”，通过人工遴选与结构化编排，将互联网上分散的高价值技术文档、性能基准测试、安全通告与架构案例整合为可检索、可追溯的参考信息库。项目不存储任何原始内容，仅提供链接与元数据标签，所有著作权归属原始权利人。

项目目标用户包括：基础架构工程师、技术选型负责人、安全合规人员以及开源贡献者。解决的核心问题包括：技术资讯过载导致的决策延迟、官方文档与社区实践之间的信息断层、以及跨团队协作时参考资料版本不一致等常见工程痛点。

## 功能概览

- **多维度标签过滤**：支持按技术领域、适用规模、成熟度等级与许可证类型进行组合筛选，快速缩小查阅范围。

- **外链健康度监控**：每日定时检测收录链接的可达性与响应时间，自动标记失效或重定向的资源，并在管理界面中高亮提示。

- **版本快照注释**：允许贡献者为每个外链添加“参考时的版本号”与“快照摘要”，便于后续回溯特定版本上下文。

- **自定义收藏集**：用户可创建私有或团队共享的收藏夹，将分散在不同分类下的链接归集到同一主题视图中。

- **全文标签检索**：基于标题、描述、标签与注释文本构建轻量级倒排索引，支持布尔表达式与前缀匹配的高级查询语法。

- **变更订阅源**：提供 Atom 与 JSON Feed 格式的订阅端点，当收录资源发生新增、更新或失效状态变化时，订阅端可自动接收推送通知。

- **批量导入导出**：支持通过 CSV 与 YAML 格式批量导入外部链接清单，并支持将筛选结果导出为结构化报告，用于内部评审归档。

- **访问统计看板**：展示链接被点击次数、热门标签排行、以及按时间维度的活跃度趋势折线图，辅助识别近期关注热点。

## 应用场景

- **技术选型前期调研** 团队在引入新的消息队列或数据库中间件时，可通过 Tangent Score 快速聚合官方基准测试报告、第三方性能对比文章以及生产环境落地案例，减少搜索引擎重复检索时间。

- **安全漏洞应急响应** 安全工程师在收到 CVE 通告后，可查找收录的官方安全公告、临时缓解措施文档与社区补丁讨论帖，快速定位可信信源，缩短风险暴露窗口。

- **新成员技术 onboarding** 团队负责人可为新入职工程师准备特定技术栈的收藏集，包含入门教程、最佳实践与内部参考架构的外链，确保学习路径统一且可追溯。

- **架构评审材料准备** 架构师在撰写评审提案时，可导出相关领域的历史决策记录、外部对比数据与行业标准规范链接，作为评审材料的附件证据链。

- **开源合规审查** 合规人员在审计项目依赖许可证兼容性时，可利用平台聚合的各开源基金会许可证解读文章与法律案例汇总，辅助进行合规风险判定。

## 快速开始

以下步骤适用于 Linux 与 macOS 开发环境，Windows 用户建议通过 WSL2 或 Git Bash 执行。

```bash
# 1. 克隆仓库
git clone https://github.com/tangent-score/tangent-score.git
cd tangent-score

# 2. 安装依赖（使用 pip 虚拟环境）
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 3. 初始化本地数据库与配置文件
cp .env.example .env
python scripts/init_db.py

# 4. 启动开发服务器
python app.py --port 8080
```

启动后访问 `http://localhost:8080` 即可进入本地实例。默认管理员账号为 `admin@tangent.local`，初始密码在首次启动时打印于终端日志中，请及时修改。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.10 或 3.11 | 运行时主语言，3.12 暂未全面测试 |
| SQLite | 3.35 及以上 | 内置数据库，用于存储链接元数据与标签 |
| Redis | 7.0 及以上 | 用于缓存访问统计与分布式锁，可选用 |
| Node.js | 18.17 及以上 | 仅用于前端资产构建，运行时可仅需编译后文件 |
| Nginx | 1.24 及以上 | 生产环境反向代理推荐，非必须但建议 |
| 系统时区 | UTC+8 或 UTC | 影响定时任务调度与日志时间戳，需预先设置 |
| 磁盘空间 | 至少 500 MB | 用于存放 SQLite 数据库与日志文件，不存储任何外链内容 |
| 内存 | 最低 1 GB，推荐 2 GB | 包含 Python 进程与 Redis 缓存占用 |
| 网络 | 出站 80/443 端口开放 | 用于健康度监控模块对外发起 HTTP/HTTPS 请求 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | `/docs/user-guide/` | 如何使用标签筛选、收藏集与订阅源，以及日常浏览操作 |
| 管理员手册 | `/docs/admin-guide/` | 如何管理外链收录、审核贡献、配置健康度阈值与查看审计日志 |
| 开发者指南 | `/docs/developer-guide/` | 如何扩展标签解析器、添加新的数据导入适配器以及调试异步任务 |
| API 参考 | `/docs/api-reference/` | 所有 RESTful 端点的请求/响应格式、鉴权方式与分页参数说明 |
| 部署手册 | `/docs/deployment/` | 生产环境容器化部署、数据库迁移与高可用配置建议 |
| 常见问题 | `/docs/faq/` | 汇总社区反馈的高频疑问，涵盖安装、迁移与性能调优 |

## 资源列表

本项目的核心价值在于对以下外部资源的持续跟踪与结构化整理。所有链接均按原始提供形式原样列出，供参阅者自行核实与访问。

技术基准与性能评测类：

- <code>qiutanzuqiubifenjiubanw.org.cn</code>
- <code>zuqiushishifen.org.cn</code>
- <code>beidanbifenjishizuqiubifen.org.cn</code>
- <code>xinqiubifen.org.cn</code>
- <code>7mbifenzuqiubifenjishi.org.cn</code>

实时数据与比分追踪类：

- <code>bifenzuqiujishi.org.cn</code>
- <code>500bifenzuqiujishi.org.cn</code>
- <code>qiutanzuqiushoujiban.org.cn</code>
- <code>zuqiubaba.org.cn</code>
- <code>zuqiubifenqiutanbifenjishi.org.cn</code>

以上资源由社区贡献者主动提交，Tangent Score 仅提供索引与分类辅助，不对链接指向的内容承担任何责任。若链接存在失效或内容不当的情况，请通过贡献渠道提交变更请求。

## 项目结构

```
tangent-score/
├── app/                            # 主应用包
│   ├── __init__.py                 # 应用工厂与配置加载
│   ├── routes/                     # 路由层，按功能域拆分
│   │   ├── api.py                  # REST API 端点（链接、标签、收藏集）
│   │   ├── web.py                  # 页面渲染路由（首页、详情、搜索）
│   │   └── admin.py                # 管理后台专用路由
│   ├── services/                   # 业务逻辑层
│   │   ├── link_service.py         # 链接增删改查与标签关联
│   │   ├── health_checker.py       # 外链健康度异步检测服务
│   │   └── subscription.py         # Atom/JSON Feed 生成与订阅管理
│   ├── models/                     # 数据模型层（SQLAlchemy ORM）
│   │   ├── link.py                 # 链接实体与状态枚举
│   │   ├── tag.py                  # 标签树与层级关系
│   │   └── user.py                 # 用户认证与权限模型
│   ├── templates/                  # Jinja2 前端模板
│   │   ├── layouts/                # 基础布局与导航组件
│   │   ├── links/                  # 链接列表、详情、编辑页面
│   │   └── auth/                   # 登录、注册与密码重置页面
│   └── static/                     # 编译后的 CSS/JS 资产（由前端构建生成）
├── scripts/                        # 运维与开发辅助脚本
│   ├── init_db.py                  # 数据库初始化与种子数据填充
│   ├── import_csv.py               # 批量导入外部 CSV 链接清单
│   └── export_yaml.py              # 导出指定收藏集为 YAML 结构化文件
├── tests/                          # 单元测试与集成测试
│   ├── unit/                       # 服务层与模型层的单测
│   └── integration/                # API 端点的端到端测试
├── docs/                           # 完整文档源文件（参考文档导航）
├── .env.example                    # 环境变量配置模板
├── requirements.txt                # Python 生产依赖
├── requirements-dev.txt            # 开发与测试额外依赖
├── Dockerfile                      # 多阶段构建镜像定义
├── docker-compose.yml              # 本地开发与集成测试编排
└── README.md                       # 本文件
```

## 贡献指南

贡献者应首先阅读并遵守项目行为准则。所有贡献均需通过 GitHub Pull Request 流程提交，并在提交前确保本地通过全部测试用例。

1. **选择或创建议题** 在 GitHub Issues 中查找带有 `help-wanted` 或 `good-first-issue` 标签的任务，或提交新议题描述您希望增加的外链类别、标签体系改进或功能增强建议，等待维护者确认后再行开发。

2. **派生仓库并创建功能分支** 将主仓库派生至个人账户下，基于 `main` 分支创建以 `feature/` 或 `fix/` 为前缀的分支名称，避免直接在派生后的 main 分支上修改。

3. **编写或调整代码与测试** 遵循项目现有的代码风格（PEP 8 与 ESLint 配置），为新逻辑补充对应的单元测试或集成测试，确保测试覆盖不低于原有水平。

4. **运行完整测试套件** 执行 `pytest tests/` 与 `npm run lint` 确认无回归错误，并检查文档中涉及的相关描述是否需同步更新。

5. **提交 Pull Request** 在 PR 描述中关联对应议题编号，概述更改内容与测试结果，等待至少一位维护者审核。审核通过后将由维护者完成合并与部署。

## 常见问题

**问：Tangent Score 是否存储外部链接的原始内容或缓存网页副本？**

答：项目明确不存储任何外部链接的原始内容、快照或缓存副本。所有收录仅包含链接地址、标题、描述与人工添加的标签注释。健康度检测仅通过 HEAD 或 GET 请求验证响应状态码与超时时间，不会持久化响应体。如需存档，请使用第三方互联网档案馆服务。

**问：如何请求添加新的外部资源链接或移除已失效链接？**

答：任何注册用户均可通过 Web 界面的“提交链接”表单提交新增请求，或通过链接详情页的“报告问题”按钮标记失效、内容变更或安全风险。提交后系统会自动创建审核工单，维护团队通常会在 48 小时内进行人工复核并给予状态反馈。

**问：生产环境部署时，健康度监控模块是否会对外部站点造成过大访问压力？**

答：监控模块采用指数退避重试策略，且每个目标链接的检测间隔默认为 24 小时，检测并发数可通过环境变量 `HEALTH_CHECK_WORKERS` 调整，默认值为 4。对于高频访问的热门链接，系统会随机延迟 0-300 秒分散请求，避免在同一时间点对所有资源发起突发流量。

## 许可证

本项目采用 MIT 许可证进行开源发布。您可以自由使用、修改、分发本软件，包括用于商业目的，但需在分发中包含原始版权声明与许可声明。本软件按“原样”提供，不提供任何明示或暗示的担保，包括但不限于适销性、特定用途适用性及非侵权性担保。详情请参阅项目根目录下的 LICENSE 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:54:35
