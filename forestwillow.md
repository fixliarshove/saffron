# Zimuziyuan Navigator

Zimuziyuan Navigator 是一个面向中文影视资源爱好者的技术导航与元数据聚合平台。本项目不提供任何视频文件的存储、下载或分发服务，仅作为公开可用网络资源的索引、分类与信息展示工具，帮助用户快速定位所需的多语言字幕文件、影视元数据信息及第三方播放辅助工具。

项目目标用户为具备基础技术操作能力的影视内容整理者、家庭影院（HTPC）用户、媒体服务器管理员以及需要批量获取影视元数据的自动化脚本开发者。通过标准化的资源链接聚合与状态检测机制，本平台旨在解决影视资源查找过程中信息分散、域名频繁变动及虚假站点难以识别等核心痛点。

## 功能概览

- **资源站点状态监控**：定期对收录的影视资源导航站进行可达性检测，标记异常域名，辅助用户筛选有效入口。
- **多维度分类索引**：按照资源类型（电影、电视剧、纪录片、动漫）、语种（中文字幕、双语字幕）及清晰度（高清、蓝光、4K）对链接进行标签化归档。
- **元数据快速查询**：集成公开的影视元数据接口，输入影片英文名或IMDb编号即可返回对应的中文译名、发行年份及剧情梗概。
- **播放辅助工具链**：提供基于开源方案的在线视频流解析建议与本地播放器配置参数参考，优化播放兼容性。
- **用户自定义订阅**：支持通过配置文件订阅特定的资源导航站分组，便于个人媒体库的自动化更新流程对接。
- **链接合规性筛滤**：内置基础规则引擎，自动过滤不符合中国大陆互联网信息内容管理规定的异常外链，确保索引内容的合规性。
- **轻量化部署能力**：项目编译为单二进制可执行文件，无外部数据库依赖，支持 Linux、Windows 及 macOS 跨平台运行。

## 应用场景

- **媒体服务器管理员维护索引**：当使用 Plex 或 Jellyfin 搭建家庭媒体服务器时，管理员可通过本项目快速获取多个备用字幕下载站点，解决因单一站点维护导致的中文字幕缺失问题。
- **自动化脚本批量获取元数据**：开发者可将本项目提供的链接列表作为输入源，编写爬虫或 API 调用脚本，批量拉取影视资源的原始发行信息，用于个人影音档案库的填充。
- **网络环境切换时的入口备援**：当主要影视导航站因网络波动无法访问时，用户可通过本平台提供的镜像索引列表快速切换至备用域名，保持资源检索行为的连续性。
- **新用户入门引导**：刚接触高清影视资源整理的新手可通过本项目的分类列表直观了解不同类型的资源获取途径，降低信息检索的学习成本。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，演示如何从源码编译并运行本项目实例。

```bash
# 1. 克隆项目仓库至本地
git clone https://github.com/zimuziyuan/navigator.git
cd navigator

# 2. 安装项目依赖（使用 Go Modules）
go mod download
go mod verify

# 3. 编译并运行开发服务
go build -o zimuziyuan-navigator ./cmd/server
./zimuziyuan-navigator --config ./configs/app.toml --port 8080
```

执行上述命令后，服务默认监听本地 8080 端口，可通过浏览器访问 `http://127.0.0.1:8080` 查看导航首页。

## 安装要求

项目运行依赖以下环境与库，请于部署前确认系统已正确安装。

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Go 编译器 | 1.21 及以上 | 项目主语言运行环境，用于源码编译 |
| Git | 2.25 及以上 | 用于克隆仓库及版本管理 |
| Make | 3.82 及以上 | 可选，用于执行自动化构建脚本 |
| 系统内存 | 256 MB 及以上 | 服务空闲运行占用约 80 MB 内存 |
| 可用磁盘空间 | 500 MB 及以上 | 包含源码、编译产物及日志存储 |
| 网络连通性 | 出站 443/80 端口开放 | 用于检测外部资源站点的可达性 |

## 文档导航

为帮助用户快速定位所需信息，项目文档按以下层面组织：

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | `/docs/user-guide/` | 如何配置订阅源、如何筛选分类、如何查看检测状态 |
| 开发者指南 | `/docs/developer-guide/` | 如何扩展新资源分类、如何自定义检测间隔、如何提交补丁 |
| 部署运维 | `/docs/operations/` | 如何容器化部署、如何进行反向代理配置、如何设置日志轮转 |
| API 参考 | `/docs/api-reference.md` | 暴露了哪些 HTTP 接口、请求与响应数据结构是什么 |

## 资源列表

本项目索引的外部影视资源导航站点按功能类别划分如下。所有链接均来源于公开互联网，项目仅提供信息汇总，不涉及任何内容分发行为。

**中文字幕影视导航（综合类）**

- <code>zhongwenzimugaoguingshipinw.org.cn</code>
- <code>gaoqingzhongwenzimudianshijuw.org.cn</code>
- <code>zaixiangaoqingzhongwenzimuw.org.cn</code>

**在线播放与日韩剧集专项**

- <code>zaixianguankanrihandianshijuw.org.cn</code>
- <code>zhongwenzimuyingshigaoqingw.org.cn</code>

**高清影视播放与资源聚合**

- <code>zaixianbofangzhongwenzimuw.org.cn</code>
- <code>gaoqingyingshimianfeiguankanw.org.cn</code>
- <code>mianfeiguankangaoqingdianyingw.org.cn</code>

**在线视频播放平台与短剧专区**

- <code>zaixianshipinbofangpingtaiw.org.cn</code>
- <code>zaixianguankanmianfeiduanjuw.org.cn</code>

## 项目结构

项目遵循 Go 语言标准布局，核心代码与资源文件组织如下：

```
zimuziyuan-navigator/
├── cmd/                                 # 可执行程序入口
│   └── server/                          # HTTP 服务主进程
│       └── main.go                      # 初始化配置、路由与启动服务
├── internal/                            # 内部私有包，不对外暴露
│   ├── collector/                       # 资源链接采集与分类模块
│   │   ├── fetcher.go                   # 实现 HTTP 请求与解析逻辑
│   │   └── parser.go                    # 解析 HTML 元数据及链接有效性
│   ├── health/                          # 站点可达性检测模块
│   │   ├── checker.go                   # 实现 TCP/HTTP 拨测与超时控制
│   │   └── status.go                    # 定义检测状态枚举与持久化结构
│   ├── model/                           # 数据实体定义
│   │   ├── resource.go                  # 资源链接结构体，含标签与分类字段
│   │   └── metadata.go                  # 影视元数据缓存结构
│   └── web/                             # Web 界面处理层
│       ├── handler.go                   # 路由控制器，处理请求与响应
│       └── template.go                  # 模板渲染函数与自定义过滤器
├── configs/                             # 配置文件目录
│   ├── app.toml                         # 主配置文件（端口、超时、日志级别）
│   └── sources.toml                     # 默认收录的资源链接列表（YAML 兼容）
├── web/                                 # 前端静态资源
│   ├── static/                          # CSS、JavaScript 及图片资产
│   │   ├── css/                         # 基于 Bulma 框架的定制样式
│   │   └── js/                          # 前端交互逻辑，含状态轮询与筛选
│   └── templates/                       # 后端渲染的 HTML 模板
│       ├── index.html                   # 导航首页，展示分类与搜索框
│       └── status.html                  # 站点检测状态面板页面
├── scripts/                             # 构建与运维辅助脚本
│   ├── build.sh                         # 跨平台编译打包脚本
│   └── healthcheck.sh                   # 服务存活检测脚本，用于 systemd
├── test/                                # 单元测试与集成测试
│   ├── collector_test.go                # 链接采集逻辑的边界测试
│   └── health_test.go                   # 检测器并发安全性与超时测试
├── go.mod                               # Go 模块依赖管理文件
├── go.sum                               # 依赖校验哈希文件
├── Makefile                             # 构建自动化任务（编译、测试、清理）
└── README.md                            # 项目说明文档（本文件）
```

## 贡献指南

欢迎开发者为本项目提交改进补丁或功能提案。为确保协作效率，请遵循以下步骤：

1.  **提交议题讨论**：在发起拉取请求前，请先于项目的议题追踪系统提交新议题，清晰说明欲修复的问题或新增的功能需求，避免重复劳动或方向偏离。
2.  **派生项目并本地开发**：将本仓库派生至个人账号下，克隆至本地并在 `develop` 分支上创建新的特性分支，命名格式为 `feature/简述修改内容` 或 `fix/简述修复内容`。
3.  **编写测试并确保通过**：所有新增功能或修复必须包含对应的单元测试用例，并确保本地执行 `make test` 后全部测试通过，覆盖率达到现有基线水平。
4.  **遵循代码规范**：代码需通过 `golangci-lint` 静态检查工具扫描，无警告或错误；提交信息采用约定式提交规范，格式为 `<类型>: <简短描述>`。
5.  **发起拉取请求**：将本地特性分支推送至派生仓库后，向本仓库的 `main` 分支发起拉取请求，并关联之前提交的议题编号，等待项目维护者审阅。

## 常见问题

**问：项目内置的资源链接检测机制是如何工作的？是否会频繁请求目标站点？**

答：检测机制默认采用指数退避策略，每 12 小时执行一次全量检测，每次检测会发送带有自定义 User-Agent 的 HEAD 请求以降低服务器负载。单个检测周期内，对同一域名的并发连接数限制为 2，避免触发目标站点的反爬策略。用户可通过配置文件调整检测间隔和超时阈值。

**问：我可以通过本项目直接播放或下载影视内容吗？**

答：不可以。本项目本身不具备视频流媒体服务或文件存储功能，所有操作仅限于对外部链接的索引展示与可达性状态反馈。用户需要自行访问第三方站点并遵守其服务条款，本项目不对第三方站点内容的合法性、可用性及完整性承担任何责任。

**问：如果某个收录的导航站域名变更了，我应该如何更新项目中的链接？**

答：您可以直接编辑 `configs/sources.toml` 文件，替换或追加新的域名条目，重启服务后生效。同时，您也可以通过 GitHub 议题或拉取请求将更新后的域名提交给项目维护者，经审核后合入主分支，使所有用户受益。项目本身不维护动态域名同步服务。

## 许可证

本开源项目遵循 MIT 许可证发布。任何人或组织均可自由使用、复制、修改、合并、出版发行、分发、再授权及销售本软件的副本，唯需在软件及衍生品中保留原始版权声明与许可声明。MIT 许可证全文请参见项目根目录下的 `LICENSE` 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:53:38
