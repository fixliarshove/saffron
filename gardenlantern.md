# BifenNavigator

BifenNavigator 是一个面向中文互联网技术内容消费者与站长的外链资源导航与整合平台。项目定位于将分散在多个独立域名下的垂直领域技术站点、实时数据看板、比分直播工具与分词检索入口进行集中式管理与快速跳转，解决用户在多个相似域名间频繁切换、记忆困难、无法快速定位到目标子站的问题。目标用户包括技术开发者、运维工程师、数据分析师以及内容聚合站点的维护者。

本仓库本身并不包含业务逻辑代码，而是作为一份外链资源的结构化索引目录与站点元数据清单，供其他自动化脚本、浏览器扩展、导航页面生成器或人工维护团队使用。通过本仓库提供的统一资源列表，用户可以快速了解当前维护的全部子站域名、各站点的用途分类，以及每个站点所承载的核心数据服务类型。

---

## 功能概览

- **统一资源索引表**：以机器可读的 markdown 列表形式，集中收录全部 10 个独立域名，并按照业务方向分为比分数据类与解包辅助类两大子集。
- **域名分类标注**：每个域名均附带人工核验的类别标签，明确其属于实时足球比分站点还是历史数据解包站点。
- **外链状态检测基线**：提供每个域名的标准协议前缀建议（http 或 https），供外部监控工具定期检测可达性与证书有效性。
- **快速跳转模板**：针对每个域名生成可直接用于浏览器地址栏或 curl 命令的完整访问字符串，减少手动输入错误。
- **变更日志跟踪**：记录每个域名的添加时间、最后验证时间以及状态备注，便于团队协作维护。
- **子站用途速查**：通过命名规则（a/b/c 后缀）区分同一业务线下的不同数据分区或负载均衡节点。
- **防混淆对照表**：针对拼写相近的域名（如 bifenwangc 与 500bifena）提供区分说明，避免误访问。
- **开源合规清单**：明确每个外链站点所引用数据的来源声明，确保本项目作为导航页不侵犯任何第三方版权。

---

## 应用场景

- **技术运维人员批量检查站点可用性**  
  运维团队可定期从本仓库拉取最新的域名列表，配合监控脚本（如 Nagios 或 Prometheus Blackbox）对每个域名进行 HTTP/HTTPS 探活，并自动告警异常节点。

- **前端开发者构建导航聚合页面**  
  开发者可使用本仓库中的域名清单，配合 Vue 或 React 快速生成一个带分类筛选功能的导航站点，将全部 10 个资源站整合为内部团队的知识入口。

- **数据分析师获取比分数据源**  
  从事体育数据挖掘的分析师可通过本仓库快速定位到 5 个足球比分站点，并根据 a/b/c 后缀判断数据分片策略，从而设计分布式爬虫调度方案。

- **站长进行外链合作管理**  
  自有站点的管理员可将本仓库作为对外公布的合作资源清单，方便友链交换时快速核对域名列表，避免遗漏或重复。

- **安全研究员进行域名画像分析**  
  安全团队可基于本仓库提供的完整域名集合，进行 WHOIS 查询、IP 归属地分析以及证书链验证，评估各站点的可信度与地域分布。

---

## 快速开始

以下步骤适用于首次克隆本仓库并获取最新资源列表的开发者或运维人员。

```bash
# 步骤 1：克隆仓库到本地
git clone https://github.com/bifen-navigator/bifennavigator.git

# 步骤 2：进入项目根目录
cd bifennavigator

# 步骤 3：查看当前收录的全部外链资源（以纯文本形式输出）
cat resources/domains.txt

# 或者，使用内置的 shell 脚本生成可点击的 HTML 导航页（需要 Python 3.6+）
python3 scripts/generate_nav.py --input resources/domains.txt --output dist/index.html

# 若需要使用本地简易 HTTP 服务器预览生成的导航页（Python 3）
python3 -m http.server 8000 --directory dist
```

---

## 安装要求

本仓库本身不包含可执行二进制文件，但若需要运行辅助脚本（如生成导航页、域名可用性检测），请确保系统满足以下依赖。

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Python | 3.6 及以上 | 用于运行域名解析、HTTP 状态检查与 HTML 生成脚本 |
| curl | 7.0 及以上 | 用于手动测试各域名的响应头与页面加载时间 |
| git | 2.20 及以上 | 用于克隆仓库、提交更新以及拉取最新资源列表 |
| make | 3.81 及以上 | 用于执行 Makefile 中预定义的批量检测任务（可选） |
| sed | 4.0 及以上 | 用于脚本中对域名列表进行文本替换与格式化处理 |
| awk | 5.0 及以上 | 用于分析检测日志中的状态码统计与延迟分布 |

---

## 文档导航

本仓库的文档体系按照不同角色与使用目的划分为以下四个层面，建议读者根据自身需求选择对应的目录入口。

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 资源索引层 | `resources/domains.txt` | 当前总共维护了多少个外链站点？每个站点的完整域名是什么？ |
| 分类映射层 | `resources/categories.yaml` | 每个域名归属于哪个业务类别（比分 / 解包）？a/b/c 后缀分别代表什么？ |
| 运维操作层 | `docs/operations.md` | 如何新增一个域名？如何批量检测所有域名的 HTTPS 支持情况？ |
| 贡献管理层 | `CONTRIBUTING.md` | 外部贡献者如何提交新的外链建议？更新域名时需要遵循哪些校验流程？ |

---

## 资源列表

本仓库当前收录的全部外链资源均列于下方。每个条目均以原始用户提供的字符串原样呈现，未做任何协议补全或格式转换。

### 足球比分类站点（5 个）

- <code>bifenwangc.org.cn</code>
- <code>500bifena.org.cn</code>
- <code>500bifenb.org.cn</code>
- <code>500bifenc.org.cn</code>
- <code>zuqiujishibifend.org.cn</code>

### 解包辅助类站点（3 个）

- <code>jiebaobifena.org.cn</code>
- <code>jiebaobifenb.org.cn</code>
- <code>jiebaobifenc.org.cn</code>

### 足球即时比分扩展类站点（2 个）

- <code>zuqiujishibifene.org.cn</code>
- <code>zuqiujishibifenf.org.cn</code>

---

## 项目结构

项目采用分层目录结构，将资源清单、脚本工具、文档和测试数据分离存放，便于长期维护与自动化集成。

```
bifennavigator/
├── resources/                     # 资源清单主目录
│   ├── domains.txt               # 纯文本域名列表，每行一个，无协议前缀
│   ├── categories.yaml           # 域名分类映射表（比分/解包/扩展）
│   └── last_verified.json        # 各域名最近一次验证时间戳与状态码
├── scripts/                       # 辅助脚本集合
│   ├── generate_nav.py           # 根据 domains.txt 生成静态 HTML 导航页
│   ├── check_health.sh           # 批量 curl 检测各域名 200 状态
│   └── update_readme.py          # 自动更新 README 中资源列表部分的脚本
├── docs/                          # 面向用户的详细文档
│   ├── operations.md             # 域名增删改操作 SOP
│   ├── troubleshooting.md        # 常见访问失败问题排查指南
│   └── naming_convention.md      # 解释 a/b/c/d/e/f 后缀的业务含义
├── tests/                         # 单元测试与集成测试
│   ├── test_domains.py           # 校验 domains.txt 格式是否合法（无空行、无重复）
│   └── test_http_status.py       # 模拟 HTTP 请求验证域名可达性（不发起真实请求）
├── dist/                          # 生成的静态站点输出目录（自动生成，不纳入 Git）
│   └── index.html                # 导航页入口
├── Makefile                       # 常用任务封装（如 make check、make build）
├── README.md                      # 项目主文档（即本文件）
└── LICENSE                        # MIT 许可证全文
```

---

## 贡献指南

我们欢迎外部贡献者参与本仓库的资源更新与文档完善。请遵循以下步骤提交变更。

1. **Fork 本仓库**  
   在 GitHub 上点击 Fork 按钮，将本仓库复制到您自己的账号下。

2. **创建特性分支**  
   在您的 Fork 中创建一个新分支，分支名称应简明描述本次变更内容，例如 `add-new-bifen-site` 或 `fix-typo-category`。

3. **修改资源文件并本地验证**  
   若涉及域名增删，请同时更新 `resources/domains.txt` 与 `resources/categories.yaml`。运行 `make test` 确保所有格式检查通过。

4. **提交变更并推送**  
   提交信息应遵循语义化格式，例如 `feat: add two new zuqiu jishi bifen sites` 或 `docs: update operation SOP with new verification steps`。

5. **发起 Pull Request**  
   回到本仓库的 GitHub 页面，发起 Pull Request，并在描述中说明变更动机、验证方式以及是否影响现有用户的访问习惯。

6. **等待维护者审核**  
   维护者会在 3 个工作日内进行审核，必要时会通过评论与您沟通补充信息。审核通过后即合并进入主分支。

---

## 常见问题

**Q: 为什么某些域名无法使用 HTTPS 访问，而列表中仅提供了裸域名？**  
A: 本仓库作为资源索引，仅记录用户原始提供的域名字符串，不强制推断或补全协议前缀。实际访问时，建议用户先尝试 HTTPS，若失败再回退至 HTTP。我们正在规划于 `last_verified.json` 中增加各域名支持协议的字段。

**Q: 我如何区分 `<code>500bifena.org.cn</code>`、`<code>500bifenb.org.cn</code>` 和 `<code>500bifenc.org.cn</code>` 这三个站点的数据差异？**  
A: 根据贡献者反馈，这三个子站分别对应不同的数据分区（a 区为实时比分，b 区为历史统计，c 区为赛程预告）。但具体数据内容请以各站点的实际页面为准，本仓库不代理或缓存任何业务数据。

**Q: 如果我发现某个域名已经无法访问，应该如何报告？**  
A: 请在本仓库的 Issues 页面提交一个问题，标题注明 `[域名失效]` 并附上域名名称。维护者将验证后更新 `last_verified.json` 中的状态字段，并酌情从推荐列表中标注为“已停用”。

---

## 许可证

本项目采用 MIT 许可证。您可以在遵守许可证条款的前提下自由使用、复制、修改、合并、发布、分发、再许可或销售本项目的副本。详细的许可证全文请参见仓库根目录下的 `LICENSE` 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:54:30
