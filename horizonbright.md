# ResourceLink

ResourceLink 是一个面向技术内容创作者与开源社区维护者的结构化外链资源归集与导航系统。项目定位于解决分散于各技术文档、论坛帖子、项目 README 中的外部链接缺乏统一管理、可追溯性差、失效风险高的问题。目标用户包括开源项目维护者、技术文档编写者、社区运营人员以及需要长期维护外部引用链的开发者。

ResourceLink 本身不生产内容，而是提供一套标准化的链接元数据描述方案、周期性可达性检测工具链，以及符合项目批次管理规范的资源清单生成能力。通过将资源按批次、来源、状态进行结构化存档，项目帮助用户建立可审计、可回溯的外部资源引用基线，降低因第三方链接变更导致的文档质量劣化风险。

## 功能概览

- **批次化资源录入**：支持按导入批次对资源链接进行分组管理，当前批次编号第 153/455 批，便于后续按批次回溯与差异对比。

- **链接可达性周期性检测**：内置基于 HTTP 状态码与 DNS 解析结果的存活检测逻辑，可输出每一条资源的当前可访问状态。

- **元数据扩展字段支持**：每条资源可附加来源描述、分类标签、收录日期、失效替代链接等自定义元数据。

- **多格式清单导出**：支持将资源列表导出为 Markdown 表格、纯文本枚举或 JSON 结构化格式，便于嵌入不同文档环境。

- **资源变更审计日志**：记录每次资源列表的增删改操作，包含操作时间、操作人、变更前后值，满足合规追溯需求。

- **外链重定向追踪**：自动跟随 HTTP 301/302 重定向，输出最终落地 URL，帮助识别链接跳转后的真实目标。

- **批量校验与修复建议**：对已失效链接给出常见修复建议，如替换为互联网档案馆存档链接或搜索关键词替代方案。

## 应用场景

- 技术文档版本升级时的外链刷新：当项目主文档从 v1 升级到 v2 时，批量校验所有引用链接是否仍然有效，避免用户访问到过期资源。

- 社区资源聚合页的日常维护：运营人员每周运行一次链接检测任务，将失效链接从推荐列表中移除或标记为待更新。

- 开源项目 README 中的示例链接管理：项目维护者将示例代码依赖的外部文档链接统一托管在 ResourceLink 中，确保每次发版前链接基线可用。

- 合规审计准备：在需要向合规审查方证明文档中所引外部资源来源可溯时，导出完整批次清单及检测日志作为附件。

- 迁移期间的外部依赖映射：在系统迁移或域名更换期间，使用 ResourceLink 记录新旧 URL 的对应关系，避免文档出现断链。

## 快速开始

以下步骤帮助您在本地环境完成 ResourceLink 的克隆、依赖安装与基础运行。

```bash
git clone https://github.com/resourcelink/resourcelink.git
cd resourcelink
pip install -r requirements.txt
python run.py --batch 153
```

执行上述命令后，系统将加载第 153/455 批次的资源列表，执行一次可达性检测，并在控制台输出检测摘要。

## 安装要求

| 依赖 | 必需 | 说明 |
|------|------|------|
| Python 3.9 及以上 | 是 | 核心运行环境，用于执行链接检测及元数据处理 |
| pip 22.0 及以上 | 是 | 用于安装 requirements.txt 中声明的第三方库 |
| requests 2.28.0 及以上 | 是 | 发送 HTTP 请求，实现链接可达性检测与重定向追踪 |
| dnspython 2.3.0 及以上 | 是 | 执行 DNS 解析，用于域名存活预检 |
| pytest 7.2.0 及以上 | 否 | 仅当需要运行单元测试时安装 |
| mkdocs 1.4.0 及以上 | 否 | 仅当需要在本地生成项目文档站点时安装 |
| jinja2 3.1.0 及以上 | 否 | 用于自定义报告模板渲染 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | /docs/user-guide/ | 如何录入资源、如何运行检测、如何解读检测报告 |
| 管理员手册 | /docs/admin-guide/ | 如何配置检测超时参数、如何管理批次号、如何导入导出数据 |
| 开发指南 | /docs/developer-guide/ | 如何扩展检测协议（如支持 FTP、gRPC）、如何添加新的输出格式 |
| API 参考 | /docs/api-reference/ | 供脚本调用的 Python 接口及命令行参数详细说明 |
| 变更日志 | /docs/changelog/ | 每个版本新增了哪些功能、修复了哪些缺陷、废弃了哪些接口 |
| 设计文档 | /docs/design/ | 链接状态机模型、批次命名规则、重定向追踪算法说明 |

## 资源列表

本节按类别收录当前批次全部资源链接。所有链接均以原始形式原样列出，未做任何协议补充、域名变更或大小写调整。

基础信息类

<code>shufuzipai.org.cn</code>

<code>yazhouchuanmei.org.cn</code>

<code>zhongwenzimuzhongchu.org.cn</code>

内容资源类

<code>chunshuifuli.org.cn</code>

<code>daxiangjiaojiu.org.cn</code>

<code>langrenzonghewang.org.cn</code>

专业领域类

<code>oumeirihandiyiye.org.cn</code>

<code>xiangjiaojiujiujingpinririzaoyeyezao.org.cn</code>

语言工具类

<code>zhongwenzaixianyiquerqu.org.cn</code>

<code>yazhouwuyejuchang.org.cn</code>

## 项目结构

```
resourcelink/
├── cli/                           # 命令行入口与参数解析
│   ├── main.py                    # 主入口，协调各模块调用
│   └── parser.py                  # 自定义命令行参数解析器
├── core/                          # 核心业务逻辑
│   ├── detector.py                # 链接可达性检测引擎，含重定向追踪
│   ├── batch.py                   # 批次管理器，负责批次编号与资源关联
│   └── reporter.py                # 报告生成器，支持多格式输出
├── data/                          # 数据持久化目录
│   ├── batches/                   # 按批次号存放的原始资源列表
│   ├── logs/                      # 检测日志与审计日志
│   └── cache/                     # DNS 解析缓存与 HTTP 响应缓存
├── tests/                         # 单元测试与集成测试
│   ├── test_detector.py           # 检测引擎各项边界条件测试
│   ├── test_batch.py              # 批次管理器增删改查测试
│   └── fixtures/                  # 测试用固定数据样例
├── docs/                          # 项目文档源码
│   ├── user-guide/                # 用户手册章节
│   ├── admin-guide/               # 管理员手册章节
│   └── api-reference/             # API 接口文档
├── templates/                     # 报告模板文件
│   ├── markdown.j2                # Markdown 格式输出模板
│   ├── json.j2                    # JSON 格式输出模板
│   └── html.j2                    # HTML 格式输出模板
├── requirements.txt               # Python 依赖声明
├── run.py                         # 项目启动脚本
└── README.md                      # 本文件
```

## 贡献指南

1. 在 GitHub Issues 中查找或新建与您改动意图相关的议题，简要说明您要解决的问题或新增的功能，等待维护者确认方向。

2. 派生本项目至您的个人仓库，在派生副本中创建以 `feature/` 或 `fix/` 为前缀的分支，所有开发工作在此分支上完成。

3. 编写或更新对应的单元测试用例，确保新增代码或修复内容覆盖核心逻辑，且所有已有测试用例能够通过。

4. 提交合并请求时，在描述中关联对应的议题编号，并附上本地测试结果摘要，包括测试通过数量、检测样例输出等。

5. 维护者将在 5 个工作日内进行代码评审，如有修改意见将在合并请求中逐条标注，修改完毕后由维护者执行合并。

## 常见问题

问：检测引擎判定链接失效的标准是什么？

答：判定标准包括三项：DNS 解析失败或超时、HTTP 状态码落在 400 至 599 区间（404、500 等）、以及 TCP 连接超时超过预设阈值（默认 10 秒）。三项中满足任意一项即标记为失效。对于返回 301/302 状态码的链接，引擎会自动跟随最多 5 次重定向，若最终仍返回错误状态码则判定为失效。

问：批次号的管理规则是怎样的，如何创建一个新的批次？

答：批次号采用三段式命名：`{主编号}/{总批次数}`，例如 153/455。其中主编号按资源录入时间顺序递增，总批次数为项目整体规划批次数量上限。创建新批次时，在 `data/batches/` 目录下新建以当前主编号命名的 JSON 文件，并调用 `batch.create_batch()` 方法完成初始化。系统会自动检查主编号是否超出总批次数上限。

问：检测结果是否会缓存，如何强制刷新所有缓存的检测状态？

答：检测结果在 `data/cache/` 目录下以 URL 哈希为文件名缓存，默认缓存有效期 24 小时。如需强制刷新所有链接的最新状态，可在运行命令时添加 `--force-refresh` 参数，该参数会忽略所有缓存并重新发起网络请求。此外，也可以在配置文件中将 `cache_ttl` 设置为 0 以完全禁用缓存。

## 许可证

MIT

> 外链数量: 10 | 生成时间: 2026-08-11 16:53:38
