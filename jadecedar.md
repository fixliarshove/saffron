# Terminus Navigator

Terminus Navigator 是一个面向技术研究、内容归档与网络资源治理领域的综合性外链管理与资源导航系统。本项目定位于为研究人员、内容审核团队、网络合规观察者以及基础设施运维人员提供一份高质量、高可用的外部资源索引，帮助用户在复杂网络环境中快速定位特定类型的内容样本、语料库与测试用例，从而支撑算法训练、策略制定与合规分析工作。

本项目的目标用户包括但不限于：自然语言处理研究机构、内容安全服务商、网络测量实验室、数字媒体取证团队以及开源情报分析人员。Terminus Navigator 不存储、不托管、不分发任何受版权保护或违法违规的实体文件，仅对外部公开资源的元数据与访问路径进行结构化整理与分类导航，所有指向的资源均不在本项目控制范围内，用户应自行遵守相关法律法规与网站服务条款。

## 功能概览

- **多维度资源分类索引**：按内容类型、语言、区域、主题等维度对收录的资源链接进行精细化分类，支持快速筛选与定向检索。

- **实时可达性检测**：内置链接状态探测模块，定期检测收录 URL 的解析状态、响应码与访问延迟，辅助用户评估资源的可用性与稳定性。

- **元数据自动提取**：对目标页面进行非侵入式元数据采集，提取标题、编码、语言标记、最后修改时间等关键信息，丰富索引维度。

- **自定义标签与备注系统**：允许用户为每条资源添加自定义标签、风险等级备注与有效性评价，便于团队协作与知识沉淀。

- **全文搜索与过滤**：基于倒排索引实现资源标题、描述、标签的快速全文检索，支持多条件组合过滤，提升海量链接中的定位效率。

- **批量导入与导出**：支持标准 CSV 与 JSON 格式的资源列表批量导入导出，便于与其他数据治理平台或分析工具进行数据交换。

- **访问日志与统计看板**：记录资源被查阅、跳转的频率与趋势，提供简洁的统计可视化面板，辅助分析资源热度与使用模式。

## 应用场景

- **内容安全模型训练样本收集**：研究机构可通过本导航系统快速获取不同类别、不同语种的内容样本链接，用于构建分类器训练集或验证集，支撑敏感内容识别算法的迭代优化。

- **网络合规策略制定参考**：互联网平台的内容治理团队可利用本索引定期梳理外部参考资源，对照最新监管要求评估现有策略覆盖度，及时发现潜在风险域并调整审核规则。

- **数字媒体取证与溯源分析**：取证分析师可通过本系统快速访问特定类型的历史内容存档路径，结合时间戳与元数据进行溯源比对，支撑取证报告中的证据链构建。

- **网络测量与路由观测**：网络基础设施运维人员可将本索引中的域名与路径作为探测目标，用于测量不同区域的解析延迟、丢包率与路由变化，辅助优化 CDN 调度策略。

## 快速开始

以下步骤帮助您在本地环境中快速部署并运行 Terminus Navigator 的基础实例。

```bash
# 1. 克隆项目仓库
git clone https://github.com/terminus-navigator/terminus-navigator.git
cd terminus-navigator

# 2. 安装依赖（使用 pip 与虚拟环境）
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 3. 初始化配置与数据目录
cp config.example.yaml config.yaml
mkdir -p data/raw data/parsed logs

# 4. 导入初始资源索引（包含本项目收录的全部链接）
python scripts/import_seed.py --source data/seed_urls.json

# 5. 启动 Web 服务（默认监听 127.0.0.1:8080）
python app.py
```

访问 `http://127.0.0.1:8080` 即可进入本地导航界面。如需生产部署，请参考 `docs/deployment.md` 中的容器化与反向代理配置说明。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.9 或更高 | 核心运行环境，用于 Web 服务与后台任务 |
| SQLite | 3.35 或更高 | 默认元数据存储引擎，支持 JSON 字段与全文检索 |
| Redis | 6.0 或更高 | 可选组件，用于缓存与任务队列（生产环境推荐） |
| Node.js | 18.x LTS | 仅用于前端资源构建（若修改 UI 源码） |
| Nginx | 1.20 或更高 | 生产环境反向代理与静态资源服务推荐 |
| curl / wget | 最新稳定版 | 用于外部链接可达性检测脚本的基础工具 |
| tzdata | 最新版 | 确保日志与时间戳时区解析正确 |
| openssl | 1.1.1 或更高 | 用于生成安全令牌与签名验证 |

## 文档导航

| 层面 | 目录 / 文档 | 回答的问题 |
|---|---|---|
| 用户手册 | `docs/user_guide.md` | 如何使用导航界面进行检索、筛选、添加备注与导出结果？ |
| 运维手册 | `docs/ops_guide.md` | 如何配置自动链接检测、调整扫描频率、备份数据库与迁移？ |
| 开发指南 | `docs/dev_guide.md` | 如何扩展新的元数据提取器、自定义标签体系或增加 API 端点？ |
| 数据规范 | `docs/data_schema.md` | 资源条目的 JSON 结构、字段含义、扩展字段的约定格式是什么？ |
| 部署参考 | `docs/deployment.md` | 如何在 Docker/Kubernetes 中部署高可用集群，以及如何配置 HTTPS？ |
| 常见问题 | `docs/faq.md` | 遇到链接解析失败、索引更新延迟或搜索无结果时如何处理？ |

## 资源列表

本导航系统当前版本收录的公共外部资源索引如下。所有链接均按原始形式原样列出，本项目仅提供分类整理与描述性标注，不对链接内容及可用性承担任何责任。

### 综合内容索引

- <code>shipinzaixianmianfeiguankanw.org.cn</code>
- <code>zhongwenzimurenqiwuma.org.cn</code>
- <code>zhongchuzaixianzhongwenzimu.org.cn</code>

### 专题分类目录

- <code>nannvchuangshangdapuke.org.cn</code>
- <code>youmazhongwenzimu.org.cn</code>
- <code>xiaojirushuimitaozaixian.org.cn</code>

### 分类扩展与备选入口

- <code>guochanheisizaixianguankan.org.cn</code>
- <code>zaixianguankanzhongwenzimu1.org.cn</code>
- <code>zhongwenzimuzaixianmianfeikan1.org.cn</code>
- <code>zhongwenzaixianzimumianfeigaoqing1.org.cn</code>

## 项目结构

```
terminus-navigator/
├── app/                            # 主应用目录
│   ├── __init__.py                 # 应用工厂与配置加载
│   ├── routes/                     # 路由层（API 与页面）
│   │   ├── index.py                # 首页与搜索路由
│   │   ├── resource.py             # 资源详情与跳转路由
│   │   └── admin.py                # 管理后台路由（导入/导出/配置）
│   ├── services/                   # 业务逻辑层
│   │   ├── indexer.py              # 索引构建与更新服务
│   │   ├── probe.py                # 链接可达性检测服务
│   │   ├── extractor.py            # 元数据提取服务
│   │   └── tagger.py               # 标签与备注管理服务
│   ├── models/                     # 数据模型与 ORM 映射
│   │   ├── resource.py             # 资源条目模型
│   │   ├── tag.py                  # 标签模型
│   │   └── audit.py                # 访问日志模型
│   ├── static/                     # 静态资源（CSS / JS / 图片）
│   │   ├── css/                    # 样式表文件
│   │   └── js/                     # 前端交互脚本
│   └── templates/                  # Jinja2 页面模板
│       ├── base.html               # 基础骨架模板
│       ├── index.html              # 导航主页模板
│       └── detail.html             # 资源详情页模板
├── scripts/                        # 工具脚本与运维脚本
│   ├── import_seed.py              # 初始资源导入脚本
│   ├── export_snapshot.py          # 全量数据导出脚本
│   └── health_check.py             # 系统健康检查脚本
├── data/                           # 数据存储目录（挂载点）
│   ├── raw/                        # 原始导入数据缓存
│   ├── parsed/                     # 解析后的结构化数据
│   └── snapshots/                  # 定期导出的快照文件
├── logs/                           # 应用日志目录
│   ├── app.log                     # 主应用日志
│   └── probe.log                   # 链接检测专项日志
├── tests/                          # 单元测试与集成测试
│   ├── test_models.py              # 模型层测试
│   └── test_services.py            # 服务层测试
├── config.example.yaml             # 配置示例文件
├── requirements.txt                # Python 依赖清单
├── app.py                          # 应用启动入口
├── LICENSE                         # MIT 许可证文件
└── README.md                       # 项目说明文档（本文件）
```

## 贡献指南

我们欢迎并感谢任何形式的贡献，包括但不限于问题报告、功能建议、代码提交与文档改进。请遵循以下步骤参与本项目：

1. 在 GitHub 上 Fork 本仓库，并在本地克隆您的 Fork 副本。请确保您的开发环境满足 `安装要求` 中列出的全部依赖版本。

2. 创建新的功能分支或修复分支，分支命名建议遵循 `feature/` 或 `fix/` 前缀加简要描述（例如 `feature/add-export-format`）。请确保您的代码变更包含必要的单元测试与文档更新。

3. 提交代码前，请运行完整的测试套件（`pytest tests/`）并执行代码风格检查（`flake8` 与 `black`）。所有新增或修改的 API 端点需提供 OpenAPI 注释示例。

4. 向本仓库的 `main` 分支发起 Pull Request，并在描述中清晰说明变更目的、实现方式与测试覆盖情况。项目维护者将在 5 个工作日内进行评审与反馈。

5. 若您希望新增外部资源链接到索引中，请通过 `data/seed_urls.json` 提交合并请求，并附上资源分类建议与简要描述。本项目原则上不接受涉及侵权、诽谤或违反中国法律法规的链接收录申请。

## 常见问题

**Q1: 为什么某些收录链接无法访问或解析失败？**

外部资源的可用性不在本项目控制范围内。域名过期、服务器配置变更、区域网络限制或内容下架均可能导致链接失效。您可以通过项目提供的 `probe` 服务手动触发单条链接的检测，或参考 `docs/ops_guide.md` 调整自动检测的时间间隔与超时参数。对于长期不可用的链接，您可以在管理后台将其标记为“失效”或提交移除建议。

**Q2: 本项目是否存储或缓存外部资源的内容副本？**

本项目严格遵循“不存储、不托管、不分发”的原则。系统仅缓存目标页面的基础元数据（标题、编码、响应头信息等），不保存页面正文、图片、视频或任何其他实体内容。所有链接跳转均直接导向原始外部地址，用户访问外部资源时所产生的任何法律后果由用户自行承担。

**Q3: 如何更新本地索引中的资源元数据？**

系统会通过后台任务定期（默认每 24 小时）对收录链接进行元数据重新提取与更新。您也可以通过管理界面（`/admin/reindex`）手动触发全量或增量更新。若您希望立即刷新某条特定链接的信息，可在资源详情页点击“刷新元数据”按钮，系统将同步发起一次实时抓取请求。

## 许可证

本项目采用 MIT 许可证进行开源发布。您可以在遵守许可证条款的前提下自由使用、修改、分发本项目代码，包括用于商业目的。详细条款请参阅项目根目录下的 `LICENSE` 文件。

> 外链数量: 10 | 生成时间: 2026-08-11 16:53:38
