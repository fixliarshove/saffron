# ReSource Navigator

ReSource Navigator 是一个面向技术决策者与内容策展人的轻量级外链资源聚合系统。项目定位为“可编程的资源导航工具”，核心能力是将大量分散的垂直领域信息源（如赛事预测、数据分析站点、专家博客）按照结构化规则进行统一收录、分类展示与快速检索。目标用户包括开源社区维护者、数据产品经理、SEO 内容研究员以及需要批量管理外部参考链接的技术团队。本项目不提供预测算法或数据分析能力，而是作为一个“资源索引的外壳”，帮助用户在不依赖复杂数据库的前提下，以纯静态方式维护一份高可读性、可版本控制的外部资源清单，并通过约定式目录结构实现资源的分类管理与版本追溯。

## 功能概览

- **批量外链收录引擎**：支持从纯文本列表、CSV 或 Markdown 表格中批量导入 URL，自动去重并校验域名可达性，生成标准化的资源条目。
- **多维度分类体系**：每个资源可归属至“赛事分析”“数据平台”“专家观点”“实时比分”等自定义分类，并支持标签系统进行交叉检索。
- **资源状态标记**：为每个链接提供“活跃”“待验证”“已失效”三种状态标记，支持手动更新与定时探测。
- **快照备注功能**：允许为每个外链添加不超过 200 字的备注说明，用于记录站点特点、更新频率或使用注意事项。
- **静态站点生成适配**：项目结构设计为与常见静态站点生成器（如 Hugo、VuePress）兼容，可直接输出为可部署的 HTML 导航页面。
- **版本化资源清单**：所有资源列表以 Markdown 文件形式存储于仓库中，便于 Git 追踪每次增删改操作，实现资源清单的变更审计。

## 应用场景

1. **垂直领域信息监控**：数据分析师可使用本项目的资源清单作为数据采集管道的前置配置源，定期抓取列表中各站点的公开数据进行趋势分析，而无需手动维护分散的收藏夹。
2. **社区文档导航建设**：技术社区或兴趣小组在构建 wiki 或文档站时，可通过本项目快速生成“相关外部资源”章节，避免在文档中散落大量裸链接，提升文档专业度。
3. **竞品信息归档**：产品团队可将竞品动态、行业报告来源、专家博客等外链按时间批次归档，每批对应一个文件夹，便于后续复盘或竞品分析报告的素材回溯。
4. **个人知识库外链治理**：知识管理爱好者可使用本项目对长期积累的浏览器书签进行结构化整理，通过项目目录树实现“按领域-按年份-按重要性”的三级整理法，并配合备注字段记录收藏理由。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，假设已安装 Git 与 Node.js（建议 v18 以上）。

```bash
# 1. 克隆项目仓库
git clone https://github.com/resource-navigator/core.git resource-navigator
cd resource-navigator

# 2. 安装依赖（使用 npm）
npm install

# 3. 运行资源清单校验与静态生成
npm run build -- --input ./data/batch_296_455.csv --output ./dist
```

若没有 CSV 输入文件，可直接使用项目内置的示例清单进行测试：

```bash
npm run example
```

该命令会在 `./dist` 目录生成一个 `index.html` 文件，包含本次批次所有外链的分类展示页面。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 18.x 或 20.x LTS | 运行时环境，用于执行构建脚本与依赖管理 |
| npm | 9.x 以上 | 包管理器，用于安装项目依赖（commander、axios、marked 等） |
| Git | 2.30 以上 | 用于克隆仓库及提交资源清单变更 |
| 网络连接 | 需能访问外网 | 用于校验外链可达性（若在内网环境可关闭校验功能） |
| 磁盘空间 | 至少 50 MB | 项目本体约 10 MB，构建产物与缓存目录建议预留 40 MB |
| 操作系统 | Linux / macOS / Windows（WSL2） | 文件路径兼容性测试覆盖以上环境 |

## 文档导航

| 层面 | 目录/文件 | 回答的问题 |
|------|-----------|------------|
| 用户手册 | `docs/usage.md` | 如何添加、编辑或删除资源条目？如何切换分类体系？ |
| 配置参考 | `docs/config.md` | 项目支持哪些构建参数？如何修改输出模板的样式？ |
| 开发指南 | `docs/development.md` | 如何扩展自定义校验规则？如何替换默认的静态渲染引擎？ |
| 维护规范 | `docs/maintenance.md` | 资源链接失效时的处理流程是什么？批次管理的命名规范如何定？ |

## 资源列表

### 赛事预测类

- <code>zuqiutuijianwangzhan.org.cn</code>
- <code>zuqiuyucezhuanjia.org.cn</code>
- <code>zuqiuyucepingtai.org.cn</code>

### 比分与赛果类

- <code>zuqiubifenyuce.org.cn</code>
- <code>zuqiuzhuanjiayuce.org.cn</code>
- <code>zuqiusaishiyuce.org.cn</code>

### 专家分析类

- <code>zuqiuzhuanjiafenxi.org.cn</code>
- <code>zuqiujinriyuce.org.cn</code>
- <code>zuqiuyucewang.net.cn</code>
- <code>zuqiuhongdanfenxi.net.cn</code>

## 项目结构

```
resource-navigator/
├── data/                           # 资源数据存储目录
│   └── batch_296_455/              # 当前批次数据（按批次号隔离）
│       ├── sources.csv             # 原始外链列表（含分类与备注）
│       └── meta.json               # 批次元信息（收录时间、校验状态）
├── src/                            # 核心源代码
│   ├── core/                       # 资源解析与校验引擎
│   │   ├── validator.js            # 域名可达性与格式校验
│   │   └── deduper.js              # 去重与别名合并逻辑
│   ├── render/                     # 静态页面渲染模板
│   │   ├── html.template.js        # 基础 HTML 骨架生成器
│   │   └── nav.component.js        # 分类导航组件
│   └── cli/                        # 命令行入口
│       └── index.js                # 主命令解析与参数处理
├── dist/                           # 构建输出目录（默认，可配置）
│   └── index.html                  # 生成的资源导航页面
├── docs/                           # 项目文档
│   ├── usage.md                    # 用户使用手册
│   ├── config.md                   # 配置参数详解
│   ├── development.md              # 二次开发指引
│   └── maintenance.md              # 日常维护流程
├── tests/                          # 单元测试与集成测试
│   ├── validator.test.js           # 校验功能测试
│   └── deduper.test.js             # 去重功能测试
├── .gitignore                      # Git 忽略规则
├── package.json                    # npm 依赖与脚本定义
├── README.md                       # 项目首页说明（本文件）
└── LICENSE                         # MIT 许可证文本
```

## 贡献指南

1. **提交资源增删改请求**：通过 Issue 提交待添加或待移除的 URL，需注明分类建议与备注理由。维护者每 48 小时合并一批次变更。
2. **本地校验与自测**：在提交 Pull Request 前，请运行 `npm run test` 确保所有单元测试通过，并执行 `npm run build -- --input ./data/your_batch.csv` 验证构建流程无报错。
3. **文档同步更新**：若本次变更涉及新功能或配置参数调整，请同步修改 `docs/` 下对应手册，并确保示例命令与实际输出一致。
4. **保持 URL 原样原则**：在 `data/` 目录的 CSV 文件中录入 URL 时，必须严格保留用户原始输入的格式（裸域名不带协议，带协议则保留协议），禁止自动补全或改写。
5. **提交信息规范**：Commit 消息请采用 `[batch-xxx] action: description` 格式，例如 `[batch-296] add: 新增三个赛事分析站点`。

## 常见问题

**Q：项目是否会自动抓取外部链接的内容或进行数据挖掘？**  
A：不会。本项目仅提供外链的静态收录与展示功能，不包含任何爬虫、数据抓取或内容分析模块。构建过程中仅进行 DNS 解析校验（可关闭），不访问页面内容，确保符合基本的数据合规要求。

**Q：如何一次性导入大量裸域名？是否需要手动添加协议前缀？**  
A：项目内置的解析器支持自动识别裸域名并补全 `http://` 用于校验显示，但在存储层面严格按照原始输入保留。用户可在 CSV 中直接写入裸域名（如 `example.com`）或带协议的完整 URL（如 `https://example.com`），系统不会修改原始字符串。

**Q：生成的静态页面是否支持移动端自适应？**  
A：默认提供的 HTML 模板基于 Flexbox 布局，已包含基础响应式样式，可在手机和平板设备上正常浏览分类列表与链接卡片。如需深度定制，可修改 `src/render/html.template.js` 中的 CSS 变量。

## 许可证

MIT

> 外链数量: 10 | 生成时间: 2026-08-11 16:54:29
